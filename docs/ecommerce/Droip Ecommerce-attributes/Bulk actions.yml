info:
  name: Bulk actions
  type: http
  seq: 6
  tags:
    - Droip Ecommerce/attributes

http:
  method: POST
  url: "{{baseUrl}}/attributes/bulk"
  body:
    type: json
    data: |-
      {
        "action": "delete",
        "ids": [
          13,
          4
        ]
      }
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/attributes/bulk"
      method: POST
      body:
        type: json
        data: |-
          {
            "action": "delete",
            "ids": [
              13,
              4
            ]
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "data": true,
            "message": "Attribute deleted successfully."
          }
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}/attributes/bulk"
      method: POST
      body:
        type: json
        data: |-
          {
            "action": "delete",
            "ids": [
              13,
              4
            ]
          }
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": false,
            "errors": {},
            "message": "Attributes could not be deleted."
          }

docs: |-
  ## Bulk Actions

  Performs a specified action (e.g., delete) on multiple resources identified by their IDs.
