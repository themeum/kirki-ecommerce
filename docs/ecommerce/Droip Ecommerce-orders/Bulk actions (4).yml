info:
  name: Bulk actions (4)
  type: http
  seq: 1
  tags:
    - Droip Ecommerce/orders

http:
  method: POST
  url: "{{baseUrl}}/orders/bulk"
  body:
    type: json
    data: |-
      {
        "action": "delete",
        "ids": [
          37,
          35
        ]
      }
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/orders/bulk"
      method: POST
      body:
        type: json
        data: |-
          {
            "action": "delete",
            "ids": [
              37,
              35
            ]
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "data": true,
            "message": "Bulk delete action performed successfully."
          }
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}/orders/bulk"
      method: POST
      body:
        type: json
        data: |-
          {
            "action": "delete",
            "ids": [
              37,
              35
            ]
          }
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": false,
            "errors": {},
            "message": ""
          }

docs: |-
  ## Bulk Actions

  Performs a specified action (e.g., delete) on multiple resources identified by their IDs.
