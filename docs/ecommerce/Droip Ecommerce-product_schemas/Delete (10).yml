info:
  name: Delete (10)
  type: http
  seq: 6
  tags:
    - Droip Ecommerce/product_schemas

http:
  method: DELETE
  url: "{{baseUrl}}/product-schemas/2"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/product-schemas/2"
      method: DELETE
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "data": true,
            "message": "Shipping profile deleted successfully."
          }
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}/product-schemas/2"
      method: DELETE
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": false,
            "errors": {},
            "message": "Shipping profile could not be found."
          }

docs: |-
  ## Delete a Product Schema

  Deletes an existing resource identified by its ID.
