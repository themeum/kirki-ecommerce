info:
  name: account/orders-html
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/account/orders-html"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
