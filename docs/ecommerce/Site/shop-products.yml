info:
  name: shop/products
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/shop/products"
  params:
    - name: format
      value: html
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
