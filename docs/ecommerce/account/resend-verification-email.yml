info:
  name: resend-verification-email
  type: http
  seq: 7
  tags:
    - account

http:
  method: POST
  url: "{{baseUrl}}/account/resend-verification-email"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
