info:
  name: Generate New Code
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/coupons/generate-new-code"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
