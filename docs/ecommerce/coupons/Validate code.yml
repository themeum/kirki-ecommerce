info:
  name: Validate code
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/coupons/{code}/validate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
