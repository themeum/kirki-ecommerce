info:
  name: Square
  type: http
  seq: 3
  tags:
    - online-payments

http:
  method: POST
  url: "{{baseUrl}}/online-payments/install"
  body:
    type: json
    data: |-
      {
        "id": "square"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/online-payments/install"
      method: POST
      body:
        type: json
        data: |-
          {
            "id": "square"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "data": {
              "id": "ec615e96-78bc-40b5-b944-dc69ae570d31",
              "name": "Square",
              "icon": "square",
              "is_enabled": null,
              "is_offline": null,
              "instructions": null,
              "config": null
            },
            "message": "Payment method installed successfully."
          }
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}/online-payments/install"
      method: POST
      body:
        type: json
        data: |-
          {
            "id": "square"
          }
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": false,
            "errors": {},
            "message": "Payment method already installed."
          }

docs: |-
  ## Install a Payment Method by ID
  Install a paymemt method by id
