opencollection: 1.0.0

info:
  name: Kirki Ecommerce
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: basic
    username: "{{auth_username}}"
    password: "{{auth_password}}"
  variables:
    - name: auth_username
      value: admin
    - name: auth_password
      value: 9UGO esUs GyFh mq9o Dg90 3l4t
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
