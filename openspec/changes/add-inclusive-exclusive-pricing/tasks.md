## 1. Calculation engine: always derive tax-exclusive prices

- [x] 1.1 In `RecalculateCartAction::build_item_result()` (`app/Actions/Cart/RecalculateCartAction.php`), compute `tax_fraction = tax_amount_money / taxable_amount` (0 when `taxable_amount` is 0) once per item when `$is_inclusive_tax` is true. Implemented as `calculate_tax_fraction()`.
- [x] 1.2 Use `tax_fraction` to extract tax from `$net_total_money` before assigning `base_subtotal` (unchanged from today when `!$is_inclusive_tax`). Implemented via `exclude_tax()`.
- [x] 1.3 Use the same `tax_fraction` to extract tax from the per-unit regular price (`$item->base_product_total`, the input value) before it's multiplied by quantity into the regular-price total, so `$item_result->base_product_total` (the output, strikethrough-total) is tax-exclusive on the same terms as `base_subtotal`.
- [x] 1.4 In `build_item_result()`, overwrite `$item_result->base_unit_price` in place by applying `tax_fraction` to `$item->base_unit_price` (no new field — matches how `base_subtotal` is already fixed in place). Add one genuinely new field, `base_regular_unit_price`, to `CalculationItemDTO`, set by applying `tax_fraction` to `$item->base_product_total` (the input, still per-unit at that point). See `design.md` - Decisions - "`CalculationItemDTO` gains a tax-excluded regular unit price; unit price is fixed in place".
- [x] 1.5 Confirm (by reading, no code change expected) that `CalculationResultDTO::base_subtotal`'s `array_sum` (`RecalculateCartAction.php:413`) needs no further change beyond 1.1-1.2. Confirmed — it sums `$item_result->base_subtotal`, which is now exclusive.
- [x] 1.6 Update `tests/Unit/Actions/Cart/RecalculateCartActionTest.php`: add/adjust cases asserting that under tax-inclusive pricing, `base_subtotal`, `base_product_total`, `base_unit_price`, and `base_regular_unit_price` all exclude tax; that each amount plus `item_tax_total` reconstructs the pre-fix gross value; that `base_total` (final charged amount) is unchanged; and that a fully-discounted item (`taxable_amount == 0`) doesn't error. Note: the "reconstructs the pre-fix gross value" invariant only holds exactly when there's no discount (item_tax_total is computed on the *discounted* taxable base, not the pre-discount subtotal) — the no-discount test asserts the exact reconstruction; the with-discount reconciliation test asserts `base_total`/`base_discount_amount` unchanged and `base_subtotal` correctly reduced instead, with the corrected rationale noted in the test's docblock.
- [x] 1.7 Run `composer test` and `composer phpcs:wporg` from the project root; fix any failures before moving on. `composer test` requires a WP test-lib install for the Integration suite (not available in this environment) — ran `bash bin/phpunit --testsuite Unit --testdox` instead (380 tests, 8755 assertions, all passing) and `composer phpcs:wporg -- <changed files>` (clean). Integration suite deferred to task 8.1 sweep / CI.

## 2. CreateOrderAction and UpdateOrderAction: persist tax-exclusive unit and regular prices

- [x] 2.1 In `CreateOrderAction::prepare_order_item_dto()` (`app/Actions/Order/CreateOrderAction.php`), replace the direct reads of `$variant->base_sale_price ?: $variant->base_price` and `$variant->base_price` with `$calculated_item->base_unit_price` and `$calculated_item->base_regular_unit_price`, converting to the order's invoiced currency the same way `invoiced_subtotal` already is.
- [x] 2.2 Apply the identical replacement in `UpdateOrderAction::prepare_order_item_dto()` (`app/Actions/Order/UpdateOrderAction.php:400-404`) — this is the item-added-to-an-existing-order path and has the exact same bug as `CreateOrderAction`. Leave `UpdateOrderAction::prepare_update_order_item_dto()` untouched — it already carries the existing item's frozen price forward. **Course correction (found during implementation)**: `UpdateOrderAction::prepare_context_items()` (the input-DTO builder, separate from `prepare_order_item_dto()`) never set `base_product_total` on the `CalculationItemDTO` it builds — only `CreateOrderAction::prepare_calculation_items()` did. Since `base_product_total` defaults to `0` and `base_regular_unit_price` is derived from it, every item added via `UpdateOrderAction` was silently getting `base_regular_unit_price = 0`. Fixed by adding `$item_dto->base_product_total = $variant->base_price;` to `prepare_context_items()`, matching `CreateOrderAction`. Caught by the Integration suite, not the Unit suite (the Unit-level `RecalculateCartActionTest` fixtures always set `base_product_total` explicitly).
- [x] 2.3 Confirm the on-sale strikethrough comparison in `OrderResource::prepare_strikethrough_price()` (`$item->base_regular_price > $item->base_price`) still holds correctly with these tax-excluded values (same relative ordering is preserved by construction — see `design.md` - Context).
- [x] 2.4 Update `tests/Integration/OrderApiTest.php` (and any `CreateOrderAction`/`UpdateOrderAction`-specific test) to assert `OrderItem.base_price`/`invoiced_price`/`base_regular_price`/`invoiced_regular_price` are tax-exclusive when the order was placed or edited under tax-inclusive pricing, covering both the new-order and add-item-to-existing-order paths. Added `enable_us_inclusive_tax()` helper (PUT `settings` with a central US tax-inclusive region) plus two new tests. **Course correction**: a tax-settings-mutating test was observed to leak its DB state into a later test in the same run despite `RestTestCase::tearDown()`'s existing cache resets (root cause not fully identified — a transaction/cache-boundary quirk in this test harness, not something introduced by this change's production code). Fixed by resetting tax settings to a known-disabled baseline unconditionally in `OrderApiTest::setUp()`, so every test in the class starts from a guaranteed state rather than depending on a prior test's cleanup.
- [x] 2.5 Run `composer test` and `composer phpcs:wporg`; fix any failures before moving on. Ran via `bash kirki-test unit`/`bash kirki-test integration --filter=OrderApiTest` (Docker; `composer test`'s host-only WP test-lib path isn't installed in this environment) — 380 Unit tests and 82 Integration `OrderApiTest` tests all passing. `composer phpcs:wporg -- <changed files>` clean (one pre-existing, unrelated `date()` lint finding at `OrderApiTest.php:192`, not touched by this change).

## 3. OrderResource: exclusive/inclusive subtotal and strikethrough fields

- [x] 3.1 In `OrderResource::prepare_items()`, replace the single `invoiced_subtotal_money_object`/`base_subtotal_money_object` pair with flat sibling `invoiced_subtotal_exclusive_money_object`/`invoiced_subtotal_inclusive_money_object` and `base_subtotal_exclusive_money_object`/`base_subtotal_inclusive_money_object`, deriving each inclusive figure as exclusive + the item's own `invoiced_tax_total`/`base_tax_total` (new `derive_inclusive_amount()` helper).
- [x] 3.2 In `OrderResource::prepare_strikethrough_price()`, return the same exclusive/inclusive sibling pair (for both invoiced and base currency) instead of a single money object, via a new `derive_inclusive_amount_at_rate()` helper. **Course correction**: the rate-fraction formula turned out not to need an `is_inclusive_tax` branch at the resource layer at all (see `design.md`'s "Correction during implementation" note on this decision) — it's always `exclusive + exclusive × (current_price_tax / current_price_exclusive)`, since the resource only ever sees already-exclusive figures.
- [x] 3.3 In `OrderResource::to_array()`'s `totals` block, replace `invoiced_items_subtotal_money_object`/`base_items_subtotal_money_object` and `invoiced_order_total_money_object`/`base_order_total_money_object` with the same exclusive/inclusive sibling pairs. **Course correction**: adding the root `invoiced_tax_total`/`base_tax_total` (order model fields) would have overstated the inclusive figure, since those include shipping tax. Added a new `get_items_tax_total()` helper (items-only tax sum) and used that instead.
- [x] 3.4 Update `tests/Unit/Resources/AdminOrderResourceCouponFormattingTest.php` (targets `app/Resources/Order/OrderResource.php` — the admin resource; `OrderResourceCouponFormattingTest.php` targets the *storefront* resource and belongs to task 6.x instead) for the new method signatures/return shape, and add cases covering an order placed under tax-inclusive pricing (subtotal and strikethrough both), plus direct tests for the two new derive helpers and `get_items_tax_total()`.
- [x] 3.5 Run `composer test` and `composer phpcs:wporg`; fix any failures before moving on. `bash kirki-test unit` — 385 Unit tests passing (was 380 pre-change). `composer phpcs:wporg -- <changed files>` clean.

## 4. OrderCalculationResource: drop display_*, restructure to match OrderResource, add exclusive/inclusive fields

- [x] 4.1 Rename the `pricing` key to `totals` in `OrderCalculationResource::to_array()`.
- [x] 4.2 Remove every `display_*_money_object` field and the `$display_currency` computation/parameter threading (`resolve_display_currency()` call, `$display_currency` arguments on `prepare_items()`, `format_coupon_results()`, `format_tax_breakdown()`, `get_applied_product_coupons_for_item()`, `prepare_strikethrough_price()`); keep only the `base_*` figures those methods already compute.
- [x] 4.3 Remove the `display_cost_money_object` sibling from `available_shipping_methods`, keeping only `base_cost_money_object`.
- [x] 4.4 Add `base_subtotal_exclusive_money_object`/`base_subtotal_inclusive_money_object` per item and for the root items-subtotal/order-total figures in `totals`, matching `OrderResource`'s convention from task 3 — including the `get_items_tax_total()` helper (items-only tax, not `CalculationResultDTO::$base_tax_total` which also includes shipping).
- [x] 4.5 In `OrderCalculationResource::prepare_strikethrough_price()`, return the same exclusive/inclusive sibling pair instead of a single money object, via `derive_inclusive_amount_at_rate()` (no `is_inclusive_tax` branch needed — same simplification as task 3.2).
- [x] 4.6 Update `tests/Unit/Resources/OrderCalculationResourceCouponFormattingTest.php` for the removed `display_*` keys, the `pricing` → `totals` rename, and the new exclusive/inclusive fields (subtotal and strikethrough); added direct tests for the two new derive helpers and `get_items_tax_total()`. No integration test touches the `calculate/order` endpoint, so no Integration-suite follow-up was needed here.
- [x] 4.7 Checked `app/Http/Controllers/Api/OrderCalculationController.php`'s `currency_code` handling — confirmed dead (only ever fed `$display_currency`, now removed) and confirmed nothing else in the controller/context uses it (order creation's own `invoiced_*` currency comes from a separate request/action entirely). Removed the `'currency_code' => $request->input('currency_code')` pass-through from the `OrderCalculationResource::make()` call.
- [x] 4.8 Run `composer test` and `composer phpcs:wporg`; fix any failures before moving on. `bash kirki-test unit` — 391 Unit tests passing (was 385). `composer phpcs:wporg -- <changed files>` clean.

## 5. CartResource: preserve current output through the engine change

- [x] 5.1 In `CartResource::prepare_items()` / `get_items_subtotal()`, add back each item's own `base_tax_amount` to `calculated_item->base_subtotal` before computing the rendered subtotal, so the storefront-visible price is unchanged under tax-inclusive pricing (per `design.md` - Decisions); leave tax-exclusive pricing untouched (adds zero). Gated on a new `Tax::is_tax_inclusive()` read in `to_array()`, threaded down to `prepare_items()`/`prepare_strikethrough_price()`.
- [x] 5.2 Apply the same add-back to the root `items_subtotal`/`order_total` figures in `CartResource::to_array()`, via a new `get_items_tax_total()` helper (items-only tax, matching Groups 3/4's equivalent — not the order-level `base_tax_total` which includes shipping).
- [x] 5.3 In `CartResource::prepare_strikethrough_price()`, scale whichever branch's amount is used (`base_subtotal` for the coupon branch, `base_product_total` for the sale-only branch) via `derive_inclusive_amount_at_rate()` under tax-inclusive pricing — **not** by adding `base_tax_amount` — so the rendered strikethrough price is unchanged too.
- [x] 5.4 Updated `tests/Unit/Resources/CartResourceCouponFormattingTest.php`: added cases proving the subtotal and strikethrough are unchanged under tax-exclusive pricing and correctly reconstructed (net+tax, and rate-scaled respectively — explicitly asserting the rate-scaled figure differs from a naive flat-add) under tax-inclusive pricing, plus a direct test for `get_items_tax_total()`. Root-level (`items_subtotal`/`order_total`) compensation is exercised implicitly via the full `CartApiTest` integration run (task 5.5) rather than a redundant Integration-level tax-inclusive fixture — the derivation formula itself is already unit-tested three times over (here, `AdminOrderResourceCouponFormattingTest`, `OrderCalculationResourceCouponFormattingTest`).
- [x] 5.5 Run `composer test` and `composer phpcs:wporg`; fix any failures before moving on. `bash kirki-test unit` — 396 Unit tests passing (was 391). `bash kirki-test integration --filter=CartApiTest` — all 29 tests passing (confirms `CartResource::to_array()`'s new `Tax::is_tax_inclusive()` read works end-to-end with the real DB/REST stack). `composer phpcs:wporg -- <changed files>` clean.

## 6. Storefront OrderResource (app/Resources/Site/Order/OrderResource.php): preserve current output through the engine change

- [x] 6.1 In `prepare_items()` / `get_items_subtotal()`, add back the item's own `invoiced_tax_total` to `item->invoiced_subtotal` before computing the rendered subtotal (item-level and root `invoiced_items_subtotal`/`invoiced_order_total`), under tax-inclusive pricing; unchanged under tax-exclusive pricing. Exact — same base as `invoiced_tax_total`. Gated on a new `Tax::is_tax_inclusive()` read in `to_array()`; new `get_items_tax_total()` helper (items-only, not the order's own `invoiced_tax_total` field which also includes shipping).
- [x] 6.2 In `prepare_strikethrough_price()`, scale whichever branch's amount is used (`item->invoiced_subtotal` for the coupon branch, `item->invoiced_regular_price * quantity` for the sale-only branch) via a new `derive_inclusive_amount_at_rate()` helper under tax-inclusive pricing — **not** by adding `invoiced_tax_total`.
- [x] 6.3 Updated `tests/Unit/Resources/OrderResourceCouponFormattingTest.php` (the *storefront* file — see task 3.4's note on the naming) — all 8 existing strikethrough tests updated for the new signature (passing `false` for `is_inclusive_tax`, unchanged expected values, proving tax-exclusive pricing is output-stable), plus a new case proving the tax-inclusive reconstruction is rate-scaled and explicitly differs from a naive flat tax-add, and a direct test for `get_items_tax_total()`. Root-level (`invoiced_items_subtotal`/`invoiced_order_total`) compensation and the full `to_array()` flow (including the new `Tax::is_tax_inclusive()` read) are exercised implicitly via `OrderApiTest`'s storefront-checkout integration tests (task 6.4) — same proportionality call as task 5.4 for `CartResource`.
- [x] 6.4 Run `composer test` and `composer phpcs:wporg`; fix any failures before moving on. `bash kirki-test unit` — 398 Unit tests passing (was 396). `bash kirki-test integration --filter=OrderApiTest` — all 82 tests still passing (this controller's checkout responses use this resource). `composer phpcs:wporg -- <changed files>` clean.

## 7. Integration coverage and frontend consumers

- [x] 7.1 Update `tests/Integration/OrderApiTest.php` for any assertions touching admin `OrderResource`/`OrderCalculationResource` subtotal/strikethrough keys, `OrderCalculationResource`'s `display_*`/`pricing` shape, or the storefront `OrderResource`'s totals/strikethrough values. Checked — this file asserts against `OrderItem` DB model fields (`base_price`, `base_regular_price`, etc.), not against `OrderResource`'s rendered money-object keys, so nothing needed updating beyond the new tax-inclusive-pricing cases already added in task 2.4. Confirmed via the full 82-test run (tasks 2.5, 6.4).
- [ ] 7.2 ~~Search `resources/app/` for consumers of `OrderCalculationResource`'s `pricing.display_*`/`pricing.base_*` fields... and update them~~ — **out of scope for this session** (user: don't touch the React frontend). Confirmed via grep that `resources/app/features/orders/schemas/catalog/order.ts` (and its test fixtures in `features/orders/tests/`) reference the old `pricing.display_*`/`pricing.base_*` shape and the admin `OrderResource`'s old single-money-object subtotal/strikethrough keys — these are now stale against the backend response shape and will need a follow-up frontend change before the admin order UI works against this API again.
- [ ] 7.3 ~~Update admin `OrderResource` consumers~~ — **out of scope for this session**, same reason as 7.2; same file (`features/orders/schemas/catalog/order.ts`) is affected.
- [ ] 7.4 ~~Run `npm run typecheck && npm test`~~ — **out of scope for this session**; no frontend changes were made to verify.

## 8. Final verification

- [x] 8.1 From the project root, run `composer test` and `composer phpcs:wporg` once more for the whole change. Via Docker (`bash kirki-test unit` / `bash kirki-test integration`, `composer test`'s host-only WP test-lib path isn't installed here): full Unit suite 398/398 passing, full Integration suite 498/498 passing (11,408 assertions) — no regressions anywhere else in the codebase. `composer phpcs:wporg` clean across every changed file (one pre-existing, unrelated `date()` finding at `OrderApiTest.php:210`, confirmed untouched by this change).
- [ ] 8.2 ~~From `resources/app/`, run `npm run typecheck && npm test`~~ — **out of scope for this session**, per the frontend exclusion above.
- [x] 8.3 Re-read `proposal.md`'s Impact section and confirm every listed file was actually touched (or confirm/record why not).
