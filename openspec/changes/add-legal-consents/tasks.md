> Build order is load-bearing — see design.md, Migration Plan. `useSettingsQuery('legal')` returns a 500 until group 1 is done, so the backend group lands first. Groups 4 and 5 can proceed in parallel once group 2 exists.

## 1. Backend: the `legal` settings group

- [x] 1.1 Add `const LEGAL_SETTINGS = 'legal';` to `app/Constants/OptionKeys.php`. This alone widens the `in:` rule in `SettingsUpdateRequest::rules()` and `SettingsController::get()`.
- [x] 1.2 Add `app/Constants/ConsentLocations.php` (`SIGNUP`, `LOGIN`, `CHECKOUT`) and `app/Constants/ConsentMethods.php` (`MANDATORY_CHECKBOX`, `OPTIONAL_CHECKBOX`, `DISPLAY_TEXT_ONLY`), both using `HasConstants` so `get_constant_values()` can feed the `in:` rules. Follow the `PageKeys` idiom; these may be `final` per CLAUDE.md since they only hold constants.
- [x] 1.3 Add `resources/data/settings/legal.json` containing `{ "consents": [] }`. Required — `AppSettings::refresh()` reads it, and without it `Settings::get('legal.consents')` returns null rather than an empty list.
- [x] 1.4 Add `app/Settings/LegalSettings.php` returning `OptionKeys::LEGAL_SETTINGS` from `get_option_key()`, modelled on `app/Settings/TaxSettings.php`.
- [x] 1.5 Add the `LEGAL_SETTINGS` case to `SettingsFactory::get_settings_instance()`. Without it `SettingsController::update()` fatals on `null->set()`.
- [x] 1.6 Add `get_legal_settings_rules()` and `get_legal_settings_filters()` to `app/Http/Requests/Settings/SettingsUpdateRequest.php` plus their two switch arms in `rules()` and `filters()`. **`'data.consents' => Sanitizer::ARRAY` must be the first filter entry** (design.md — Order the sanitizer filters). Every consent field needs a filter entry or it is silently dropped before storage. Do **not** add a `prepare_for_validation()` arm — it would break that method's `default: return;` guard.
- [x] 1.7 Add the `legal` case to `app/Resources/SettingResource.php`: `array_values()` the consents so they serialise as a JSON array, and expose `is_registration_enabled` from `Utils::registration_enabled()`.
- [x] 1.8 Add round-trip integration tests to `tests/Integration/SettingsApiTest.php`: a two-consent array survives a PUT/GET round trip with `locations` intact (this is the test that catches a missing filter entry); PUT-two-then-PUT-one leaves one; an unknown location returns 422; an empty array clears the list.
- [x] 1.9 Verify: phpcs clean on all files touched here (`composer phpcs:wporg` exits 1 on pre-existing findings in untouched files — it already did at HEAD). 7 new integration tests pass; the `SettingsApi` suite's one failure (`Update general settings persists order number...`) pre-dates this change. JS: 1068 tests pass, typecheck clean. **Note:** `vendor/bin/phpunit` cannot run on the host (no WP test library) — use `bash kirki-test integration` / `unit`, which runs it in Docker.

## 2. Backend: consent resolution and token rendering

- [x] 2.1 Add `find_published_by_slugs(array $slugs)` to `app/Services/PageService.php` — one batched query on `post_type = page`, `post_status = publish`, `where_in('post_name', $slugs)`.
- [x] 2.2 Add `app/Services/LegalConsentService.php` with `get_renderable(string $location)`, `get_mandatory_ids(string $location)`, and `render_message(string $message)`. Consumers must never read raw settings themselves.
- [x] 2.3 Implement `render_message()` escape-first: `nl2br(esc_html($message))`, then `preg_replace_callback('/\{([a-z0-9_]+)\}/i', ...)`. Resolve `{privacy_policy}` → slug `privacy-policy` → `<a href target rel>` built from `esc_url` + `esc_html`. An unresolved token renders as humanised escaped text, never the literal token and never an empty link.
- [x] 2.4 Collect every token across every consent for a location in a single `preg_match_all` pass and resolve all slugs in one query — per-token lookups would make three consents into six queries per checkout render.
- [x] 2.5 Add `tests/Unit/Services/LegalConsentServiceTest.php`: token → anchor; unknown token → humanised text; merchant-typed `<script>` appears escaped; adjacent and repeated tokens both resolve.
- [x] 2.6 Verify: phpcs clean on every file touched here. Unit suite 201 tests OK (8 new). Added `esc_url`/`get_permalink` stubs to `tests/Support/StubsWordPressFunctions.php` so the render test stays a pure unit test.

## 3. Admin: schemas and registrations

- [x] 3.1 Added `features/settings/legal/schemas/catalog/legal.ts`. **Correction:** `consents` had to be `.nullish()`, not `.default([])` — `getSettings` indexes `SettingsSchemaMap` with a generic key, and a required output property broke that union's inference (typecheck error TS2345). Leniency was the house rule anyway; consumers do `?? []`.
- [x] 3.2 Add `features/settings/legal/schemas/forms/consent-form.ts`: three booleans (`show_on_signup`/`show_on_login`/`show_on_checkout`), a `.superRefine` requiring at least one, and a `.transform()` flattening to `locations[]`. Write its payload test in the same task — `features/settings/legal/tests/schemas/forms/consent-form.test.ts`, covering booleans → ordered `locations`, the all-false issue, required messages, and defaults.
- [x] 3.3 Add `features/settings/legal/schemas/forms/legal-settings-form.ts` exporting the `LegalSettingsFormPayload` type only — there is no page-level `useForm` here, so a schema would be dead weight that `knip` would flag.
- [x] 3.4 Register the read schema: `legal: LegalSettingsSchema` in `SettingsSchemaMap` (`resources/app/schemas/catalog/settings.ts`, 8 → 9 sections), carrying the same `no-restricted-imports` disable comment its siblings use.
- [x] 3.5 Register the write payload: export `LegalSettingsFormPayload` from `features/settings/index.ts`, then add `legal:` to `SettingsPayloadMap` in `resources/app/services/settings.ts` (7 → 8 keys) and update its "only N sections" comment.
- [x] 3.6 Verify: typecheck clean, 1076 tests pass (133 files). Had to update the deliberate tripwire in `schemas/catalog/settings.test.ts` that pins the map to eight sections — now nine, with `legal` listed.

## 4. Admin: the Legal page

- [x] 4.1 Add `features/settings/legal/lib/token-insert.ts` with `slugToToken(slug)` and `insertTokenAtCursor(value, token, selection | null)`. Clamp both offsets to `value.length` — a stored selection can outlive a `form.reset()` that shortened the message. `selection === null` must append at the end.
- [x] 4.2 Add `features/settings/legal/tests/lib/token-insert.test.ts`: no selection appends; caret at 0 prepends; mid-string splice returns `start + token.length`; a non-empty selection is replaced; a stale selection past the end is clamped without mangling; slug → token conversion including already-underscored and single-word slugs.
- [x] 4.3 Add `features/settings/legal/components/fields/consent-message-field.tsx`. Location is forced by `eslint.config.js` — `Controller` may only be imported under `components/form/**` or `features/**/components/fields/**`. Snapshot `selectionStart`/`selectionEnd` into a ref on select/keyUp/click/blur; merge the textarea ref *after* `{...field}` so RHF's own ref survives; call `field.onChange(text)` before restoring the caret in a `requestAnimationFrame`; prevent Radix's `onCloseAutoFocus`; `type="button"` on the trigger and every option.
- [x] 4.4 Wire the "+" popover to the existing `usePagesQuery` from `features/settings/advanced/services/page-settings.ts`, called with **exactly** `{ status: 'publish' }` — `pageKeys.lists()` omits params from the key, so different params from another caller would serve the wrong cached list. Leave a comment saying so at the call site.
- [x] 4.5 Add `features/settings/legal/pages/consent-dialog.tsx`, structured like `shipping-box-dialog.tsx`: title `TextField`, three `CheckboxField`s under a "Display on" label, the consent message field, a method `SelectField`, and a Cancel / "Save changes" footer. No toggle in the header, no Remove button in the footer.
- [x] 4.6 Add `features/settings/legal/skeletons/legal-settings-skeleton.tsx` and `features/settings/legal/lib/utils.ts` (`upsertConsent`, `toggleConsent`, `removeConsent` as pure list helpers) with tests for the helpers under `features/settings/legal/tests/lib/utils.test.ts`.
- [x] 4.7 Add `features/settings/legal/pages/legal-settings.tsx`. Hold the list in local state synced from `useSettingsQuery('legal')` and persist through one `commit(updater)` that rolls back on failure. **`commit` must take an updater and read previous state from a ref**, not close over the rendered value — the delete flow's `onSuccess` fires up to 5s later, inside the undo window (design.md — Immediate persistence).
- [x] 4.8 Build the list: `StackedItems` rows with location chips in a `Flex` marked `data-right-text="true"` and actions in an `ActionGroup` — the existing CSS in `stacked-items.tsx` already swaps one for the other on hover/focus/open, so do not hand-roll hover CSS. Disabled rows get persistent muting plus a "Disabled" badge. Use the shared `components/ui/empty-state.tsx` for "Consents will be shown here" — not the hand-rolled copies in `tax-profile.tsx`/`schema-profile.tsx`.
- [x] 4.9 Wire delete through `useConfirmDelete` plus `dispatchToastMessage('delete', ...)` with an undo action, following `shipping-box.tsx` including its `removedIds` display filter.
- [x] 4.10 Render a warning on the page when any enabled consent targets signup while `is_registration_enabled` is false. The consent must still save — the warning describes the condition, it does not block configuration.
- [x] 4.11 Register the page: `LegalSettings: defineRoute('/legal')` in `config/route-config.ts`; a lazy route entry in `features/settings/routes.tsx`; and fix the placeholder link in `features/settings/lib/utils.tsx` so Legal points at `LegalSettings` instead of `AdvancedSettings`.
- [x] 4.12 Add the `legal` entry to the `SETTINGS_PAGES` map in `scripts/build-settings-search-index.mjs` and put `data-search-id="legal.consents"` + `data-search-keywords` on the card. Not optional — the crawler warns on any translated `<Card>` without a search id, and until this lands `nav.legal` is filed under the Advanced page because both entries resolve to the same `routeKey`.
- [x] 4.13 Verify: typecheck clean, lint clean (`eslint` on the whole feature), 1098 tests pass (135 files, up from 1068/132). `npm run search:index` reports 44 documents, 0 warnings, and `nav.legal` now resolves to `/settings/legal` instead of being filed under the Advanced page.

## 5. Storefront: checkout

- [x] 5.1 Pass `get_renderable(ConsentLocations::CHECKOUT)` into the view data from `SiteController::checkout_page()`. Views must not call `Settings::get` directly — that convention holds across the whole storefront.
- [x] 5.2 Add `resources/views/site/checkout/parts/consents.php` and include it from `checkout.php` immediately above the Place Order button. Reuse the existing `.kecom-checkbox` markup; mark mandatory consents as required; echo rendered messages through `wp_kses_post()`; wrap the whole part so nothing renders when there are no consents.
- [x] 5.3 Add the shared error block using the `.kecom-alert-error` class — that is exactly what `scrollToFirstError()` queries, so scroll-to-error works with no extra wiring.
- [x] 5.4 Add consent styles to the checkout SCSS (label layout, required marker, link treatment).
- [x] 5.5 Ship `checkout_consents` as `{id, method}` only from `PageInlineScript::set_checkout_page_data()`, and add the type to `resources/site/ts/types.ts`. The rendered HTML is already in the DOM — duplicating it into the inline script doubles the payload for nothing.
- [x] 5.6 Add `consents` and `consentError` state to `resources/site/ts/components/checkout.ts`, seeded in `init()`; gate inside `placeOrder()` right after the shipping-method check, mirroring the existing `shippingMethodError` pattern; send accepted ids in `orderData`; add a `consents` branch to `handleApiErrors()` so a server rejection lands inline rather than in a toast. **Do not extend `form.ts`** — it has no checkbox path, and the consents live in the `checkout()` scope, not the contact form's.
- [x] 5.7 Add the server-side rule to `app/Http/Requests/Order/OrderCreateRequest.php` as a **non-wildcard closure** (only those are evaluated when the key is absent, so a stale storefront bundle is still rejected), opening with `if (!empty($data['is_manual'])) { return true; }`, plus `consents` filter entries.
- [x] 5.8 Added six order tests. **Correction:** the manual-order guard asserts "no consent rejection" rather than a 201 — manual order creation already 500s at HEAD (`test_store_order_returns_201` fails identically without this feature), so pinning 201 would make this guard fail for an unrelated bug. It asserts the consent rule never rejects an admin order, which is what it exists to prove.
- [x] 5.9 Verify: phpcs clean on every file touched here. Integration suite 368 tests / 7 errors / 52 failures — **identical error and failure counts to the pre-change baseline of 355**, so all 13 new tests pass and nothing regressed. Storefront bundle builds clean (`npm run build` in resources/site, tsc included).

## 6. Storefront: wp-login.php

- [x] 6.1 Added both render hooks. Their shared markup, inline `<style>` and consent-reading live in a new `app/Concerns/RendersLoginConsents.php` trait (the `Concerns/` sub-namespace is this codebase's convention) so the two render hooks and the two validation filters share one implementation.
- [x] 6.2 Add `app/Hooks/Filters/ValidateRegisterConsents.php` (`registration_errors`, 3 args) returning one aggregate `WP_Error` in WordPress's own register-form style.
- [x] 6.3 Add `app/Hooks/Filters/ValidateLoginConsents.php` (`authenticate`, 3 args) at **priority 30** — after `wp_authenticate_username_password` at 20, or a wrong password gets reported as a consent failure. Bail early on `is_wp_error($user) || !($user instanceof WP_User)` and when no `log` field was posted; without that second guard the filter fires on REST application passwords and XML-RPC and locks people out.
- [x] 6.4 Register all four classes in `config/hooks.php`.
- [x] 6.5 Read consent submissions through `Superglobals`, never `$_POST` directly, per CLAUDE.md.
- [x] 6.6 Verify: phpcs clean on all six files. Added `tests/Integration/LegalConsentHooksTest.php` — 10 tests covering the three `authenticate` guards (priority 30, credential error wins, non-form auth untouched), plus location/method filtering and the single aggregate registration error. Integration suite now 378 tests with errors/failures still 7/52, unchanged from baseline. JS: 1098 pass, typecheck clean.

## 7. Documentation

- [x] 7.1 Write `docs/legal-consents.md` following the structure of `docs/settings-search.md` — note that `docs/cache.md`, which CLAUDE.md §6 names as the template, does not exist. Sections: quick start; the three consent methods and what each enforces; page tokens including what happens when a page is unpublished, deleted, or renamed; where consents appear; **what is and is not recorded — state plainly that no per-customer or per-order acceptance record exists**; requirements and limitations (`users_can_register`, the `registration_errors` coverage gap, the `authenticate` guard); and a closing "Where this differs from a full consent-management platform" section covering the absent audit trail, consent-text versioning, re-consent prompting, and per-customer export.
- [x] 7.2 Flagged to the user in the final report: CLAUDE.md §6 points at `docs/cache.md`, which does not exist. `docs/settings-search.md` was used as the structural template instead. CLAUDE.md left unedited.
- [x] 7.3 Verified each concrete doc claim against shipped code rather than assuming: the 5s undo window, the red required marker, the registration warning, scroll-to-error on the consent gate, the Disabled badge, and the two service method names all match.

## 8. Final verification

- [x] 8.1 Full suite green. PHP unit 201 OK. PHP integration 378 tests, errors/failures 7/52 — **identical to the pre-change baseline of 355/7/52**, so all 23 new PHP tests pass with zero regressions. phpcs: verified line-by-line against the diff that **no finding falls on a line this change added**. JS: typecheck clean, eslint clean, 1098 tests pass. Storefront bundle builds clean.
- [x] 8.2 Spec scenarios split into what is proven and what is not, rather than claiming a walkthrough I did not perform (CLAUDE.md §0 forbids browser verification here).

  **Proven by automated tests:** consent validation and round-trip; token → link, unresolved token → humanised text, merchant markup escaped; server-side checkout enforcement including the `is_manual` admin exemption, disabled consents, optional consents and consents targeting another location; all three `authenticate` guards (priority 30, credential error wins, non-form auth untouched); the registration block and its single aggregate error; location flattening; cursor-insertion logic.

  **Not machine-verified — needs the user's eyes:** the visual result at checkout (consent above Place Order, hover-revealed row actions, disabled-row muting), the browser-side Alpine gate (only the server-side rejection is automated), the `+` page-picker interaction in a real browser (jsdom's `setSelectionRange` support is shallow, so the pure helper was tested instead), and `wp-login.php` appearance.
