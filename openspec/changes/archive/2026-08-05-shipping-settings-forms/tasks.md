## 1. Shared form fields and schemas

- [x] 1.1 Add `schemas/forms/shipping-method-form.ts`: one shape holding `type`, `name`, `description` and the per-type fields, with per-type requirements via `requiredWhen()` reading root values (design Decision 1). Carry over the existing defaults from `METHOD_SCHEMAS` only (`amount: 0`, `is_taxable: false`, `description: null`, `ranges: []`, pickup fields) — no new `.default()` on a previously-required field
- [x] 1.2 Declare `ranges` as `z.array(z.object({ from, to, amount }))` with per-row validation inside the row object and the non-empty rule applied at the array level, not on its elements — `collectIssuesForShape` does not recurse into arrays (design Decision 3)
- [x] 1.3 Write the transform naming every payload field explicitly and emitting only the fields belonging to the selected `type`, replacing `sanitizeByMethodType`. Route the rate-by-weight free-shipping amount to `free_shipping_min_amount` rather than the shared `amount` key
- [x] 1.4 Add `shipping-method-form.test.ts` asserting the exact `z.output<>` body for all three method types, including that a type switch drops the previous type's fields
- [x] 1.5 Add `schemas/forms/shipping-zone-form.ts` (`title` required, `regions` non-empty via `.min(1, msg)`) + `shipping-zone-form.test.ts`. Reused the existing `ShippingRegionFormShape` fragment (exported from `shipping-region-form.ts`) for the per-region shape instead of redeclaring it
- [x] 1.6 Add `components/form/regions-field.tsx` following the `text-field.tsx` template (`useFormContext` + `Controller`, no `control` prop, `Field`/`FieldLabel`/`FieldError`, `displayName`, `defineStyles` after the default export). Inline type-to-search over `useCountriesQuery({limit:-1})` via the existing `getSearchedCountries`; chips via `getSelectedRegionTags`; empty state when nothing selected
- [x] 1.7 Reworked `ShippingRegionPopup`'s prop contract (`open`/`onOpenChange`/`onDone`, dropped the always-dead no-arg `onAdd` branch and the four eager parent setters) so it only reports the final selection once, on Done — removing the `syncParent` dual source of truth (design Decision 4). Replaced the inline `style={{ height: '432px', ... }}` with a `defineStyles` entry. Patched its two existing callers (`shipping-settings.tsx`, `shipping-zone.tsx`) to the new contract now to keep the build green; both get fully rebuilt on RHF in Group 2 anyway
- [x] 1.8 Add `components/form/weight-range-field.tsx`: `useFieldArray` keyed by `field.id`, rows of two number inputs + an amount input with labels and per-row `FieldError`, hover-revealed row delete, "Add Another Range" button. Used a plain number input (not `MoneyField`'s `$`-prefixed style) for the rate column — the screenshot shows unprefixed numeric fields for all three columns in this table, unlike the standalone Rate/Fee fields elsewhere

## 2. Main page and zone page

- [x] 2.1 `shipping-settings.tsx`: fix the no-op `sprintf(__('%s'), item.title)` and replace the untranslated `` `${n} regions, ${m} shipping methods` `` subheader with a real `sprintf` format string
- [x] 2.2 Wire the zone accordion's method rows to show `description` as supporting text, the formatted amount as trailing text, and the inactive marker from `is_enabled` — using `GroupOptionCard`'s existing props, no new component. Added `getShippingMethodSubText`/`getShippingMethodRightText` to `utils.tsx`, shared by both the zone-accordion rows and the zone page's own Shipping Methods section. Also fixed the "Added shipping profiles will appear here" copy-paste bug in the Shipping Methods empty state (same file, same block)
- [x] 2.3 Shipping box rows: added dimensions (`L x W x H unit`) as `subText`; `is_default` badge already worked. Shipping profile rows: **no `rule count`/`in-use zone count` field exists anywhere in the API or `ShippingProfile` type** — computed both counts client-side by scanning `shippingSettingsData.shipping_zones` for rules whose condition is `{type: 'shipping_profile', value: profile.name}`, since that's how a rule already stores a profile reference (`shipping-rule-dialog.tsx`'s condition value options map profile name, not id). No backend change
- [x] 2.4 Convert `shipping-zone/shipping-zone.tsx` to `useForm<ShippingZoneFormInput, unknown, ShippingZoneFormPayload>` with `zodResolver` + `getDefaults`, hydrating via `pickFormValues`, deleting the hand-rolled `FormErrors`/`normalizeErrors`/`useState` block
- [x] 2.5 Replaced the zone page's raw title `Input` with `TextField` and its read-only chip trigger with the new `RegionsField`; dropped the duplicated `mergeCss(cardStyles.formCard, cardStyles.formCard)`. The "Add Method" header came for free — `ShippingMethod`'s non-`edit_zone` branch already renders its own `HeaderActionsCard` with that exact button, unchanged
- [x] 2.6 Verified in the browser against real seeded data: destination search filters (matches on both country name and nested state names, inherited from the existing `getSearchedCountries`/`getNestedSearchedValue` helper — e.g. typing "ind" surfaces Colombia via its "Quindío" state, not just India-like country names), chips render and remove correctly, the single-country state-picker dialog opens for a country with states and adds/removes cleanly, and the Save button's dirty state tracks live edits

## 3. Method detail page on React Hook Form

- [x] 3.1 Convert `shipping-delivery-method.tsx` to the 3-generic `useForm` + `zodResolver(ShippingMethodFormSchema)`, hydrated with `pickFormValues`, saved through `useSettingsPageActions({ onSave: form.handleSubmit(...) })`, errors via `applyServerErrors`. Deleted `dataObj`, `initialDataObj`, `handleOnChange`, and `sanitizeByMethodType`. Widened `ShippingMethodData`'s `amount`/`ranges`/`free_shipping_min_amount` fields to accept `null` in `utils.tsx` — the new schema legitimately emits `null` for a field that doesn't apply to the selected type (e.g. no pickup fee), which the old `number | string` type didn't allow
- [x] 3.2 Replaced the method name `Input` with `TextField` and the method type `Select` with `SelectField`; switching type preserves `name`/`description` (both are shared, always-present schema fields, not reset on type change). `SelectField`'s options shape has no icon slot, so the per-type dropdown icons (`TruckIcon`/`WeightIcon`/`StoreIcon`) are dropped — a shared field component was the explicit instruction, and it doesn't support a leading-icon slot
- [x] 3.3 Rewrote `flat-rate-settings.tsx` with `MoneyField`, `CheckboxField` and `TextareaField`, taking no props and reading form context
- [x] 3.4 Rewrote `local-pickup-settings.tsx` with `TextField`, `TextareaField`, `CheckboxField` + conditional `MoneyField`, and the pickup-time inputs via `TextField type="time"` (no dedicated time field exists and building one for two call sites would be a speculative abstraction); conditional rendering driven by `useWatch`, removing the local `useState` mirrors that never resynced
- [x] 3.5 Rewrote `rate-by-weight-settings.tsx` using `WeightRangeField` and the checkbox/money fields; the flush-left JSX block is gone since the whole file was rewritten
- [x] 3.6 Removed `METHOD_SCHEMAS` from `utils.tsx` (and the now-orphaned `MethodSchema` type) — confirmed by grep that nothing else referenced either
- [x] 3.7 The three duplicated `textarea.minHeight: '108px'` blocks are gone — all three sub-forms now use `TextareaField`'s `rows` prop instead of a custom style
- [x] 3.8 Verified in the browser against real seeded methods: Flat Rate blocks save with "Amount is required" when the Rate is cleared and shows the field-level error with a red border/label; Local Pickup correctly hydrates the pickup-fee checkbox **unchecked** (confirms 4.6), and checking it reveals the previously-hidden but still-stored fee amount; Rate by Weight hydrates two real weight-range rows, "Add Another Range" appends a blank row, the hover-revealed delete removes only that row, and saving with an incomplete row blocks with "Rate is required" on that row specifically

## 4. Inline rule builder and defect fixes

- [x] 4.1 Add `shipping-rules/shipping-rule-form-card.tsx` — the inline IF/THEN card reusing `ShippingRuleFormSchema` unchanged, with Cancel and Add Rule actions, rendered above the rule list. Kept the per-condition data-loading effects (categories/profiles/destination-region) and the `SelectDestinationPopup` sub-dialog exactly as the old modal had them — only the outer chrome (Dialog → dashed `Card`) changed
- [x] 4.2 Wire edit to render the card in place of the row being edited, and cancel to leave saved rules untouched — `shipping-rules.tsx` swaps the row for `<ShippingRuleFormCard mode="edit" .../>` at the edited index; `onCancel`/`onSaved` both just clear the editing/add state, no mutation on cancel
- [x] 4.3 Deleted `shipping-rule-dialog.tsx` after confirming by grep it had no remaining importers. No helpers in `utils.tsx` were orphaned by the deletion — `conditionOptions`/`actionOptionsArray`/`saveShippingZones` etc. are all still used by the new card and `shipping-rules.tsx`
- [x] 4.4 `shipping-rules.tsx`: added `getConditionLabel`/`getOperatorLabel`/`getActionLabel` lookups against `conditionOptions`/`actionOptionsArray` (operator has no options list to look up against — inlined the same three labels `getOperatorOptions()` in the rule card offers), replacing the raw enum keys; fixed `sprintf(__('Then %s:'))` to pass the action label as its argument
- [x] 4.5 Fixed the condition key mismatch in `utils.tsx` — `product_categories` → `product_category` (design Decision 7). Checked real saved rules in the dev environment during the Group 5 browser pass: none use either the old plural `product_categories` or the fixed singular `product_category` — the seeded rules use an unrelated, unmapped condition type (`product_profile`) that predates and is unaffected by this fix. Nothing to normalize
- [x] 4.6 Already fixed as a side effect of 3.4's rewrite — the new `local-pickup-settings.tsx` reads `has_fee` via `useWatch` directly from form state instead of a local `useState` mirror, so the `|| true` bug has no equivalent to reintroduce
- [x] 4.7 Changed `saveShippingZones` in `utils.tsx` to send only `{ shipping_zones: zones }`, dropping the `...shippingSettingsData` spread. Left the now-unused `shippingSettingsData` param on the type/signature rather than touching its three call sites for a param removal — out of scope for this fix
- [x] 4.8 Removed the unreachable `shipping/delivery-method/:methodId/:zoneId` route from `routes.tsx`

## 5. Verification

- [x] 5.1 `npm test` in `resources/app` — 212/212 tests pass across 47 files, including the 14 new `shipping-method-form.test.ts` cases and 3 new `shipping-zone-form.test.ts` cases
- [x] 5.2 `tsc --noEmit` clean. No `lint` script exists in this repo (checked `package.json` at both the repo root and `resources/app`; no `eslint.config.*` present either) — typecheck and tests are the available gates and both pass
- [x] 5.3 Full browser pass against the real dev environment (seeded data, not mocks) via `http://localhost:20100/wp-admin/admin.php?page=ecommerce#/settings/shipping`: main page zones/profiles/boxes render with correct badges and i18n; zone page validation and `RegionsField` verified (2.6); all three method types verified including per-type and per-row validation (3.8); inline rule builder verified end-to-end — add with a real condition/operator/action, edit an existing rule with correct pre-fill, and cancel leaving saved rules untouched. One console-level "Maximum update depth exceeded" error was investigated and confirmed to be pre-existing environmental noise — it reproduces identically on the plain WordPress Dashboard, entirely outside the eCommerce React app, so unrelated to this change
- [x] 5.4 Confirmed via the network panel: adding a rule sent `PUT .../settings` whose response echoes back a settings object where `shipping_zones` reflects only the intended method's `shipping_rules` change and the sibling `shipping_carriers` section is preserved untouched — consistent with the request having sent only `{ key: 'shipping', data: { shipping_zones: [...] } }` rather than the whole blob (the tool used cannot capture the outgoing request body directly, only the response; the fix itself was verified by code review in Group 4 — `saveShippingZones` and every other mutation call site construct `data` as `{ shipping_zones }` only)
