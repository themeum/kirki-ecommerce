## 1. Restrict `CreateCustomerAction` to a single public method

- [x] 1.1 In `app/Actions/Customer/CreateCustomerAction.php`, fold `provision()`'s body back into `execute()` (replacing the `$customer = $this->provision(...)` call with the logic it wrapped) and delete the standalone `provision()` method.
- [x] 1.2 Restore `execute()`'s docblock to describe the method directly (drop the "single transaction" cross-reference to a separate `provision()` method that no longer exists). Docblock already described `execute()` directly with no reference to `provision()` — nothing to change.
- [x] 1.3 Run `composer test` and confirm the suite passes (no other caller referenced `provision()`). Note: `CreateOrderAction` still called `provision()` at this point (task group 2 not yet applied), so this run correctly failed with "undefined method provision()" — expected until 2.6. Re-verified together with 2.6 after group 2 landed.

## 2. Resolve checkout customer before order transaction and cart calculation

- [x] 2.1 In `app/Actions/Order/CreateOrderAction.php::execute()`, move the customer-resolution block (`if (empty(...) && !$dto->is_manual && !empty($dto->created_by)) { ... }`) from inside `DB::begin_transaction()` to the top of the method, before `prepare_calculation_context_dto($dto)` is called.
- [x] 2.2 Change the resolution to mutate `$dto->customer_id` directly (instead of `$create_order_dto->customer_id`, which doesn't exist yet at that point in the method).
- [x] 2.3 In `resolve_checkout_customer_id()`, change the call from `$this->create_customer_action->provision(...)` to `$this->create_customer_action->execute(...)`.
- [x] 2.4 Remove the now-redundant customer-resolution branch and its guarding conditional from inside the `DB::begin_transaction()` try block, along with the `$create_order_dto->customer_id` assignment it performed (confirm `prepare_create_order_dto()`'s existing `$order_dto->customer_id = $context->customer_id ?: null;` still resolves correctly since `$dto->customer_id` is now set before the context is built). Confirmed correct.
- [x] 2.5 Confirm `UniqueConstraintViolationException` handling in `resolve_checkout_customer_id()` is unchanged and still reachable at the new call site. Confirmed — only the `provision()` → `execute()` call changed; the surrounding try/catch is untouched.
- [x] 2.6 Run `composer test` and confirm the suite passes. Verified via `kirki-test integration --filter="OrderApiTest|CustomerApiTest"` — all 32 tests pass except the pre-existing, unrelated `test_create_refund_on_order` failure (confirmed identical on unmodified `HEAD`).

## 3. Update and add integration test coverage

- [x] 3.1 In `tests/Integration/OrderApiTest.php`, update `test_checkout_failure_leaves_no_customer_or_address_behind` (and rename it, e.g. `test_checkout_failure_leaves_provisioned_customer_behind`) to assert the new behavior: after an order failure triggered post-provisioning (e.g. the existing insufficient-stock scenario), the `Customer` record, its WordPress user, and its addresses ARE persisted, while no order is created.
- [x] 3.2 In `tests/Integration/OrderApiTest.php`, add a test covering the `first_time_buyer_only` coupon scenario from `specs/checkout-customer-provisioning/spec.md`: an authenticated user with no existing `Customer` record applies a first-time-buyer coupon during checkout and it is not rejected with "please login" (scope narrowed during implementation — see design.md "Correction during implementation"; this does not assert prior-order-count enforcement, which is a separate pre-existing gap).
- [x] 3.3 In `tests/Integration/OrderApiTest.php`, add a test covering the per-customer-usage-limit coupon scenario from the same spec: an authenticated user with no existing `Customer` record applies such a coupon during checkout and it is validated against their resolved `customer_id` (usage count 0 < limit) rather than rejected with "please login" for a missing one. (Customer-include-list coupon dropped from scope during implementation — see spec.md note; not observable for a genuinely first-time buyer.)
- [x] 3.4 Run `composer test` and confirm the full suite passes, including the new and updated tests. Full suite (`kirki-test all`, 306 tests): 3 pre-existing failures unrelated to this change (`test_create_refund_on_order`, 2 CartApiTest failures) — confirmed identical on unmodified `HEAD` via `git stash`. All tests touched by this change (CreateCustomerAction, CreateOrderAction, OrderApiTest, CustomerApiTest) pass.
