## 1. Populate customer_order_count during checkout calculation

- [x] 1.1 In `app/Actions/Order/CreateOrderAction.php::prepare_calculation_context_dto()`, after `$context->customer_id` is set, populate `$context->customer_order_count` when `$context->customer_id` is truthy, using the same query as `CalculationContextDTO::from_cart()` / `OrderCalculationController::get_order_count()`: the resolved customer's orders count, excluding `FulfillmentStatus::CANCELLED` and `FulfillmentStatus::RETURNED` (both already imported/available in this file). Leave it at the DTO default (`0`) for guest checkout (`customer_id` falsy).
- [x] 1.2 Run `composer test` and confirm the suite passes. Verified via `kirki-test integration --filter="OrderApiTest|CustomerApiTest"` — 34 tests, only the pre-existing unrelated `test_create_refund_on_order` failure.

## 2. Add test coverage for repeat-customer rejection

- [x] 2.1 In `tests/Integration/OrderApiTest.php`, add a test: an authenticated user with an existing `Customer` record and at least one prior non-cancelled/non-returned order applies a `first_time_buyer_only` coupon during checkout. **Premise corrected during implementation** (see design-equivalent note below — this change has no design.md, so recorded here): the request is NOT rejected with 422. `RecalculateCartAction::get_discount_result()` catches all `Throwable`s from coupon validation and silently drops the discount instead of failing checkout, so an ineligible coupon just doesn't apply — order still returns 201. Added `test_checkout_ignores_first_time_buyer_coupon_for_repeat_customer`, asserting on `totals.base_shipping` (stays at the full $10 flat rate, i.e. the free-shipping coupon did not take effect) rather than on HTTP status or `discount_details` (`discount_details` cannot be used either — separate pre-existing bug found while debugging this: `DiscountService::calculate()` sets `discount_details` to a raw `Coupon` model object, which is lost/nulled on persist to the order's JSON column, unrelated to this fix, out of scope, not tracked further here). Verified with a red/green check: the test fails without this change's fix (`git stash` on `CreateOrderAction.php` alone) and passes with it.
- [x] 2.2 Confirm the existing `test_checkout_accepts_first_time_buyer_coupon_for_new_customer` and `test_checkout_accepts_customer_usage_limit_coupon_for_new_customer` (added in `decouple-checkout-customer-provisioning`) still pass. **Found and fixed while doing this**: both only asserted HTTP 201, which — given the swallow-and-drop behavior above — passes regardless of whether the coupon was actually applied, so they didn't discriminate fixed from broken for *that* earlier change either. Strengthened both to assert `totals.base_shipping === 0.0` (free shipping actually took effect), same reasoning/signal as 2.1.
- [x] 2.3 Run `composer test` and confirm the full suite passes, including the new test. Full suite (`kirki-test all`, 307 tests): 3 pre-existing failures unrelated to this change (`test_create_refund_on_order`, 2 `CartApiTest` failures) — same as confirmed in the prior change. Everything else passes.
