## 1. Dependency and types

- [x] 1.1 `npm i @tanstack/react-table` in `resources/app/`
- [x] 1.2 Augment `ColumnMeta` in `resources/app/global.d.ts` (alongside the existing Emotion `Theme` augmentation) with `alignment?: TableAlignment` and `cssOverride?: CSSObject`. It goes here so the required `interface` keyword stays out of files linted by `consistent-type-definitions: 'error'`. Note: the `declare module` target is `'@tanstack/table-core'` (the module that actually declares `ColumnMeta`; `@tanstack/react-table` only re-exports it via `export *`), matching TanStack's own documented pattern, with a side-effect `import '@tanstack/react-table'` kept to ensure types load. The generic parameter names (`TData`, `TValue`) must match table-core's own names verbatim — TypeScript's declaration-merging for generic interfaces requires identical type-parameter names across merged declarations, not just identical arity/constraints; using renamed params (e.g. `_TData`/`_TValue`, to dodge the unused-vars lint rule) silently corrupted the merge into a 4-type-argument interface. Suppressed the resulting lint warning with a targeted `eslint-disable-next-line` instead.
- [x] 1.3 Reshape `components/data-table/types.ts`: add `DataTableSelectionState = { selectedIds: string[]; isAllMatchingSelected: boolean; selectedCount: number }`; keep `DataTableItem` and the row-action config types; delete `DataTableColumn`, `DataTableBulkApplyPayload`, `DataTableRowActionsResolver` and `EMPTY_PAGE`
- [x] 1.4 Verify: `npm run typecheck && npm test` in `resources/app/` — the 3 current consumers will now fail to compile; that is expected until `data-table-migrate-catalog`

## 2. Params binding hook

- [x] 2.1 Create `resources/app/hooks/use-data-table-params.ts` wrapping `use-list-params.ts`, returning `{ params, pagination, sorting, onPaginationChange, onSortingChange, selectionResetKey, setParam, setParams }`
- [x] 2.2 Implement the page-base conversion in both directions: `pageIndex = params.page - 1` on read, `page = pageIndex + 1` on write. Missing or `1` in the URL means index `0`
- [x] 2.3 Implement the sort conversion: `params.sort_by` + `params.sort_order` ↔ `SortingState` (`[{ id, desc }]`). Empty sort state means no `sort_by` update
- [x] 2.4 Derive `selectionResetKey` from the search term and filter keys only — destructure out `page`, `limit`, `sort_by`, `sort_order` before serialising, mirroring the signature the deleted selection context computed
- [x] 2.5 Wrap `onPaginationChange` / `onSortingChange` in `useCallback` over the stable `setParams` so they keep stable identities across URL changes. They must accept TanStack's updater-function form as well as a plain value
- [x] 2.6 Export from `resources/app/hooks/index.ts`
- [x] 2.7 Write `resources/app/hooks/use-data-table-params.test.tsx` (needs a router wrapper): page 1 and absent page both yield index 0; page 4 yields index 3; reporting index 3 writes page 4; sort round-trips; changing search or a filter changes `selectionResetKey`; changing page, limit or sort does not; handler identities are stable across a URL change. Note: `.tsx` not `.test.ts` — it renders a JSX `MemoryRouter` wrapper via `renderHook`, which needs the jsdom ("dom") Vitest project; `**/*.test.ts` runs under the node-only "unit" project in this repo's `vitest.config.ts`.
- [x] 2.8 Verify: `npm run typecheck && npm test` in `resources/app/`

## 3. DataTable core

- [x] 3.1 Rewrite `components/data-table/data-table.tsx` around one `useReactTable` instance: `manualPagination: true`, `manualSorting: true`, `manualFiltering: true`, `getCoreRowModel()` only, caller-supplied `pageCount`, and controlled `state: { pagination, sorting, rowSelection, columnPinning, columnVisibility }`. Do not add `getFilteredRowModel` / `getSortedRowModel` / `getPaginationRowModel` — they would re-process already-server-processed rows. `columnPinning`/`columnVisibility` are plain pass-through props (no `onChange` required from callers) since nothing inside this component ever calls `column.pin()`/toggles visibility itself — they're declarative feature configuration, wired to no-op `onColumnPinningChange`/`onColumnVisibilityChange` handlers to satisfy TanStack's controlled-state contract.
- [x] 3.2 Default `getRowId` to `String(row.id)`. Without this TanStack keys selection by array index and selection silently follows positions across pages
- [x] 3.3 Render the header from `table.getHeaderGroups()`; a header is sortable only when `column.getCanSort()`, and reports through `column.toggleSorting()`. Carry `meta.alignment` onto the header cell. Also set `enableSortingRemoval: false` at the table level so a header toggle always lands on asc/desc, never clears — matching the bridge hook's "empty sort state is a no-op" rule and the old `Sorting` component's two-state toggle UX.
- [x] 3.4 Render the body from `table.getRowModel().rows` via `flexRender`, carrying `meta.alignment` and `meta.cssOverride` onto each cell. Keep the existing row-click and per-cell click-stops-propagation behaviour
- [x] 3.5 When `enableRowSelection`, prepend a `{ id: 'select', size: 40, enableSorting: false }` column: header checkbox from `getIsAllPageRowsSelected()` with `isPartialChecked` from `getIsSomePageRowsSelected()`, cells from `row.getIsSelected()`. Keep `onlyCheckbox` on both cells and stop click propagation. Both checkboxes also OR in `isAllMatchingSelected` so the cross-page select-all mode reads as fully checked, not merely indeterminate.
- [x] 3.6 Implement `isAllMatchingSelected` as component state: setting it clears the per-row selection map and makes the reported `selectedCount` the total; toggling any individual row leaves the mode
- [x] 3.7 Report `{ selectedIds, isAllMatchingSelected, selectedCount }` through `onRowSelectionChange` from an effect keyed on the selection state — not during render, or a caller storing it in state can loop
- [x] 3.8 Clear the selection in an effect on `selectionResetKey` change. Paging and sorting must not clear it
- [x] 3.9 Loading: when `isLoading`, replace the body with one row containing `ui/spinner`, `colSpan={table.getVisibleLeafColumns().length}`. Header, `toolbar`, `filterBar` and pagination all stay mounted; pagination is passed `disabled`
- [x] 3.10 Empty: when not loading and there are no rows, render `emptyState ?? <DataTableEmptyState />` in a spanning row using the same visible-column count
- [x] 3.11 Pinning: for pinned cells apply `position: sticky` plus background and z-index through `scopedMerge`, and the offset from `column.getStart('left')` / `column.getAfter('right')` through the `style` prop, since those are runtime-computed
- [x] 3.12 Compose the layout: `Card` › `CardContent` › toolbar-or-selection-bar › `filterBar` › `Table` › `Pagination` (from `ui/pagination`, this change's first consumer), with `hidePagination` to opt out. Pagination bar composed inline in `data-table.tsx` from the `ui/pagination` primitives (`PaginationPageSelect` + prev/numbered-links/next via `utils/pagination`'s `getPageItems`) — no separate `data-table-pagination.tsx` file, since the primitives are consumed directly.
- [x] 3.13 Delete `data-table-context.tsx`, `data-table-selection-context.tsx`, `data-table-header.tsx`, `data-table-body.tsx`, `data-table-toolbar.tsx`, `data-table-slots.tsx`, `data-table-pagination.tsx`. The `useListParams()` call inside the selection context is deleted, not relocated
- [x] 3.14 Verify: `npm run typecheck && npm test` in `resources/app/`. Also ran `npm run lint`, which surfaced a real bug in the task 1.2 `ColumnMeta` augmentation (not merely a lint nit): using renamed generic params (`_TData`/`_TValue`) to dodge the unused-vars rule silently broke TypeScript's declaration merging (it requires identical type-parameter names, not just arity/constraints), corrupting `ColumnMeta` project-wide into a bogus 4-type-argument interface that `tsc`'s `skipLibCheck` was hiding everywhere except at this file's explicit usage. Fixed at the source in `global.d.ts` (see task 1.2 note) rather than working around it here.

## 4. Empty state, selection bar, row actions

- [x] 4.1 Create `components/data-table/data-table-empty-state.tsx` composing the existing `ui/empty-state.tsx` (`{ icon?, text, cssOverride? }`), with a translated default message via `__()` and domain `kirki-ecommerce`
- [x] 4.2 Create `components/data-table/data-table-selection-bar.tsx` presenting the selected count, the bulk-action select and apply control, and the select-all-matching offer. Show that offer only when the total exceeds the rows currently shown. Clear the selection after the caller's `onBulkApply` resolves
- [x] 4.3 Use a translated, `sprintf`-interpolated selected-count string — `BulkActionHandler`'s existing `${itemCount} items selected` is untranslated and must not be carried over. (Format specifier fixed to `%s` in task 5.7 below — see that note.)
- [x] 4.4 Keep `data-table-row-actions.tsx` as a presentational component for features to render inside their own actions column; drop the resolver plumbing. Retain the `data-action-group` attribute so the primitives' hover-reveal applies. No edit needed — this file was already presentational (`{ edit, actions, actionCssOverride }` props in, no resolver), and `data-action-group="true"` comes from the `ActionGroup` primitive it renders, unchanged.
- [x] 4.5 Rewrite `components/data-table/index.ts` to export `DataTable`, `DataTableRowActions`, `DataTableEmptyState` and the types. Remove the `Object.assign` namespace (`Filter`, `SelectionFilter`, `FilterBar`, `Pagination`)
- [x] 4.6 Verify: `npm run typecheck && npm test` in `resources/app/`

## 5. Component tests

- [x] 5.1 Create `components/data-table/data-table.test.tsx`. Render outside any router in at least one test, to prove the component no longer depends on one
- [x] 5.2 Loading: with `isLoading` **and rows still present** (the `keepPreviousData` shape), the spinner shows and the stale rows do not; the header cells remain in the DOM; toolbar and filter bar remain; the pagination bar remains but is inert
- [x] 5.3 Empty: no rows and not loading shows the default empty state; a supplied `emptyState` replaces it; loading with no rows shows the spinner and not the empty state
- [x] 5.4 Paging: activating a page control reports a 0-based `pageIndex`, and the rendered rows do not change until new `data` is supplied
- [x] 5.5 Sorting: activating a sortable header reports that column's `id`; a column without `enableSorting` renders no sort affordance and does not report
- [x] 5.6 Selection: no selection column unless `enableRowSelection`; selecting a row reports its entity id; re-supplying the same entities in a different order keeps them selected; a partially selected page shows the indeterminate header state
- [x] 5.7 Select-all-matching: choosing it reports `isAllMatchingSelected` with `selectedCount` equal to the total; the offer is absent when the total does not exceed the rows shown; `onBulkApply` receives both the ids and the flag. Fixed a real bug found while writing this test: the selection bar's `sprintf` calls used `%d` for the counts, but this codebase's `sprintf` (and its test mock) only substitutes `%s` — `ui/pagination.tsx`'s existing `sprintf(__('of %s', ...), totalPages)` does the same for a number. Switched all three format strings in `data-table-selection-bar.tsx` to `%s`.
- [x] 5.8 Selection lifecycle: changing `selectionResetKey` clears the selection; changing the page does not
- [x] 5.9 Pinning and visibility: a pinned column's cells are `position: sticky`; a hidden column renders no header and no cells, and the loading row's `colSpan` counts only visible columns. The sticky-position assertion needs the render wrapped in `#wpbody-content .kirki-ecommerce-root` — `scoped()`/`scopedMerge()` (theme/mixins.ts) nest every style under that ancestor selector for specificity, so without it in the test DOM the rule never matches and `getComputedStyle` reports the default `static`.
- [x] 5.10 Verify: `npm run typecheck && npm test` in `resources/app/`

## 6. Final verification

- [x] 6.1 Run `npm run typecheck && npm run lint && npm test` in `resources/app/`. All 82 test files / 570 tests pass. `lint` reports 54 errors, all inside the 3 unmigrated consumers, plus one pre-existing, unrelated `object-shorthand` error in `features/products/components/product-form/sections/right-panel/categories/categories.tsx` (confirmed via `git status` — untouched by this change, not introduced by it, left alone per surgical-changes scope).
- [x] 6.2 Confirm the only remaining typecheck failures are the 3 unmigrated consumers (`product-table.tsx`, `order-table.tsx`, `coupon-table.tsx`) still using the old API. Any other failure is in scope for this change. Confirmed — every `tsc` error is inside those three files.
- [x] 6.3 Confirm no file under `components/data-table/` imports `use-list-params`, `useMarkList`, `react-router` or `BulkActionHandler`. Confirmed via grep — no matches.
- [x] 6.4 Confirm no `as unknown as` cast remains in `components/data-table/`. Confirmed via grep — no matches.
