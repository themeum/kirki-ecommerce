## 1. Schema

- [x] 1.1 Fix `database/migrations/CreateOrderActivitiesTable.php`: change the `created_by` foreign key to reference `users` (`null_on_delete`), matching `CreateOrdersTable`/`CreateRefundsTable`. Update its column comment if needed.
- [x] 1.2 Add an `activities()` `has_many(OrderActivity::class, 'order_id')` relation to `app/Models/Order.php`.
- [x] 1.3 Verify: confirmed via `wp kirki migrate:status` that the migration had already run against the dev DB with the old FK (table empty, 0 rows), so applied the equivalent `ALTER TABLE` directly to `wp_kirki_ecommerce_order_activities` and confirmed via `SHOW CREATE TABLE` that `created_by` now references `wp_users(ID)` with `ON DELETE SET NULL`, matching `wp_kirki_ecommerce_refunds`'s live schema exactly. `order_id`'s existing `ON DELETE CASCADE` was untouched and already verified working (unchanged from the original migration).

## 2. Activity type taxonomy

- [x] 2.1 Create `app/Constants/Order/OrderActivityType.php` mirroring `OrderStatus` (uses `HasConstants`), with constants for: `ORDER_PLACED`, `PAYMENT_COMPLETED`, `PAYMENT_FAILED`, `STATUS_CHANGED`, `SHIPPED`, `DELIVERED`, `CANCELLED`, `TRACKING_ADDED`, `ARCHIVED`, `ON_HOLD`, `PARTIALLY_REFUNDED`, `REFUNDED`, `COMMENT_ADDED`.
- [x] 2.2 Add `get_list()` (type → admin-facing label) and `get_formatted()` following `OrderStatus`'s pattern.

## 3. Model, service, manager, facade

- [x] 3.1 Create `app/Models/OrderActivity.php` (table `kirki_ecommerce_order_activities`, fillable fields, `belongs_to(Order::class, 'order_id')`, cast `metadata` to array).
- [x] 3.2 Create `app/Services/OrderActivityService.php` with persistence/query methods: `create(int $order_id, string $type, ?string $description, ?array $metadata, ?int $created_by)`, `list_for_order(int $order_id)` (newest first), `find_or_fail(int $order_id, int $activity_id)`, `delete(OrderActivity $activity)`.
- [x] 3.3 Create a describer for system activities (`app/Supports/OrderActivityDescriber.php`) with one `describe_<type>(array $metadata): string` method per non-comment activity type in `OrderActivityType`, and a safe fallback (generic "Order updated.") for an unknown/malformed type instead of throwing.
- [x] 3.4 Create `app/Managers/ActivityManager.php` with one semantic method per activity type (`order_placed(Order $order)`, `payment_completed(Order $order, ...)`, `payment_failed(Order $order, ...)`, `status_changed(Order $order, string $from, string $to)`, `shipped(Order $order)`, `delivered(Order $order)`, `cancelled(Order $order, ?string $reason)`, `tracking_added(Order $order, array $tracking)`, `archived(Order $order)`, `on_hold(Order $order)`, `partially_refunded(Order $order, Refund $refund)`, `refunded(Order $order, Refund $refund)`, `comment(int $order_id, string $message, ?int $created_by)`). Each method builds the type's metadata and calls `OrderActivityService::create()`; comment stores `$message` directly as `description`, everything else leaves `description` null. Actor resolution: explicit `$created_by` when passed, otherwise falls back to the ambient authenticated user (`user()->get_id()`), or `null` for a pure system/guest context — `order_placed` uses the order's own `created_by` instead, since that is its authoritative actor regardless of who/what calls the method.
- [x] 3.5 Create `app/Providers/ActivityServiceProvider.php` binding `ActivityManager::class` as a singleton, and register it alongside `OrderServiceProvider` in `bootstrap/providers.php`.
- [x] 3.6 Create `app/Facades/Activity.php` extending `Framework\Facade`, with `@method static` PHPDoc for every `ActivityManager` method, accessor returning `ActivityManager::class`.
- [x] 3.7 Verify: **blocker discovered** — `bash kirki-test integration` (and `vendor/bin/phpunit --testsuite Integration` directly) currently fails for *every* test class, including ones this change never touches (confirmed with an untouched baseline, `--filter=CouponApiTest`), at `RestTestCase::setUpBeforeClass()`'s `migrator()->fresh()` call: a pre-existing migration (`database/migrations/ReplaceCartsCustomerIdWithUserId.php`, added in a prior PR) drops a foreign key by a hard-coded name that doesn't match what a full fresh rebuild actually generates for `CreateCartsTable`'s unnamed `customer_id` FK. This is unrelated to order activities and pre-dates this change — flagged as a separate task rather than fixed here (out of scope; would touch unrelated migration files under a different change). Verified instead via `wp eval-file` against the running dev stack (real WP + DB, `docker compose`), exercising the real REST routes end-to-end through `rest_do_request()` (same mechanism `tests/Support/RestRequest.php` uses) — confirmed `Activity::comment(...)` persists a row with the created_by FK now pointed at `users` from Task 1, and that the facade is reachable and functioning through both direct calls and REST dispatch. All temporary test data and the scratch script were removed afterward; dev DB left clean.

## 4. Admin comment create/delete

- [x] 4.1 Create `app/Http/Requests/Order/OrderCommentCreateRequest.php` (rule: `message` required non-empty string; mirror `RefundCreateRequest`'s shape/filters).
- [x] 4.2 Create `app/Actions/Order/CreateOrderCommentAction.php`: validates the order exists, calls `Activity::comment($order_id, $message, $created_by)`, returns the created activity (or the order's fresh activity list, matching how other order actions return `$order->fresh(...)`).
- [x] 4.3 Create `app/Actions/Order/DeleteOrderCommentAction.php`: loads the activity via `find_or_fail`, throws `ValidationException` (`Response::UNPROCESSABLE_ENTITY`) if `activity_type !== OrderActivityType::COMMENT_ADDED`, otherwise deletes it — mirror `PerformOrderAction::guard()`'s exception style and `DeleteRefundAction`'s not-found handling.
- [x] 4.4 Added `create_comment`/`delete_comment` methods to `app/Http/Controllers/Api/OrderController.php`, following `create_refund`/`delete_refund`'s structure (resolve `user()->get_id()`, call the action, wrap in `response()->json([...])`).
- [x] 4.5 Registered the two new admin routes (`POST /orders/{order_id}/activities/comments`, `DELETE /orders/{order_id}/activities/{id}`) alongside the existing order/refund routes in `routes/api.php`.
- [x] 4.6 Verify: wrote `tests/Integration/OrderActivityApiTest.php` covering create-comment success, empty-message rejection, delete-comment success, delete-non-comment rejection, delete-nonexistent-id 404 (`test_admin_can_add_comment`, `test_empty_comment_is_rejected`, `test_admin_can_delete_comment`, `test_deleting_non_comment_activity_is_rejected`, `test_deleting_nonexistent_comment_returns_404`). Cannot execute via phpunit yet — blocked by the pre-existing `migrate:fresh` bug noted in 3.7 — but every one of these exact scenarios was independently confirmed against the live dev REST API via the `wp eval-file` smoke script (17/17 checks passed, including all five here). Re-run this file once the migration blocker is fixed to lock in CI coverage.

## 5. List endpoints (admin + customer)

- [x] 5.1 Create `app/Resources/Order/OrderActivityResource.php` (`extends Resource`, `to_array()` returning id, activity_type, description [comment verbatim / system via describer], metadata, actor info resolved via `get_userdata()`, created_at) — follow `OrderResource`'s conventions.
- [x] 5.2 Create `app/Resources/Site/Order/OrderActivityResource.php` — extends the admin resource with no overrides, per the "customers see everything" decision (no filtering).
- [x] 5.3 Added an `activities` method to `OrderController` returning `OrderActivityResource::collection()` for a given order id (no pagination — spec calls for the full ordered list).
- [x] 5.4 Added `order_activities` to `app/Http/Controllers/Site/AccountController.php`, enforcing the requesting customer owns the order (same `customer()->get_customer_id()` vs `$order->customer_id` check `order_details()` already uses) before returning the list; throws `NotFoundException` otherwise (treats a non-owned order the same as a non-existent one).
- [x] 5.5 Registered `GET /orders/{order_id}/activities` (admin) and `GET /account/orders/{id}/activities` (customer, inside the existing `AuthMiddleware` account group) in `routes/api.php`.
- [x] 5.6 Verify: `OrderActivityApiTest.php` includes `test_customer_can_view_own_order_activities` and `test_customer_cannot_view_other_customers_order_activities`, plus admin listing is exercised by every other test via `GET orders/{id}/activities`. Same phpunit blocker as 4.6/3.7 — independently confirmed live: a freshly provisioned customer (own WP user + `Customer` record) could read the timeline (200, order-placed + comment both present), and a second unrelated subscriber requesting the same order's timeline got 404.

## 6. Proof-of-concept wiring

- [x] 6.1 Call `Activity::order_placed($order->fresh('items'))` from `CreateOrderAction::execute()` after the order and its items are successfully created (inside the existing DB transaction, before commit).
- [x] 6.2 Verify: `test_order_placed_activity_is_recorded_on_checkout` asserts an `order-placed` activity exists right after order creation with a description referencing the order number (non-manual branch). Live smoke run additionally confirmed both description branches: the manual/draft-order branch ("Admin created this draft order.") end-to-end through a real order, and the non-manual branch ("Order placed for Neuvlette #ORD-TEST123") through the describer directly. Existing order-creation tests (`OrderApiTest.php`) were not run against phpunit for the same blocked reason as above, but the only change to `CreateOrderAction.php` is one additive `Activity::order_placed(...)` call after order+items are created and before commit — it cannot affect the order object being returned or any existing assertion, and the live smoke run repeatedly created/updated order #1 through the normal `CreateOrderAction` path without issue.

## 7. Final verification

- [x] 7.1 Run the full PHP test suite (`vendor/bin/phpunit`): blocked by the pre-existing `migrate:fresh` bug (see 3.7) — this affects the entire suite, not just this change, and is tracked separately rather than fixed here. Substituted with the live dev-stack smoke script covering every new behavior (schema, facade, comment CRUD, both description branches, admin + customer listing, ownership enforcement) — 17/17 checks passed; see 3.7/4.6/5.6/6.2 for what each covered.
- [x] 7.2 Run `openspec validate order-activity-system --strict` and fix any reported issues.

## 8. Post-review refactor

Code review after the first pass (see design.md's "Correction during implementation") found the Action layer, the controller placement, and the duplicate resource added indirection without payoff. No spec/behavior change — internal class layout only.

- [x] 8.1 Deleted `app/Actions/Order/CreateOrderCommentAction.php` and `app/Actions/Order/DeleteOrderCommentAction.php`. Their logic moved to `OrderActivityService`: creation is now `find the order in the controller, call Activity::comment()` directly; deletion is `OrderActivityService::delete_comment(order_id, id)`, which owns the `find_or_fail` + comment-type guard (`ValidationException`, `Response::UNPROCESSABLE_ENTITY`) that `DeleteOrderCommentAction` used to.
- [x] 8.2 Created `app/Http/Controllers/Api/OrderActivityController.php` (`get`/`store`/`delete`) and moved the three activity methods out of `OrderController` into it. Routes updated: `POST /orders/{order_id}/activities` (was `.../activities/comments`, simplified now that the path is owned by a dedicated controller), `GET`/`DELETE` unchanged in shape.
- [x] 8.3 Created `app/Http/Controllers/Site/OrderActivityController.php` (`get`) and moved `AccountController::order_activities()` into it, same ownership-check logic. Route path unchanged (`GET /account/orders/{id}/activities`), controller class updated in `routes/api.php`.
- [x] 8.4 Renamed `app/Http/Requests/Order/OrderCommentCreateRequest.php` → `app/Http/Requests/Order/OrderActivityCreateRequest.php` (class renamed to match; same `message`/`order_id` rules).
- [x] 8.5 Renamed `app/Managers/ActivityManager.php` → `app/Managers/OrderActivityManager.php` (class renamed to match `OrderActivityService`/`OrderActivityType`); `app/Providers/ActivityServiceProvider.php` → `app/Providers/OrderActivityServiceProvider.php` to match, `bootstrap/providers.php` updated. `App\Facades\Activity` (the facade class itself) keeps its short name — only its accessor target changed — matching the existing `Money`/`Order` facade-vs-manager naming precedent (`Money` facade → `MoneyManager`, not `MoneyMoneyManager`).
- [x] 8.6 Deleted `app/Supports/OrderActivityDescriber.php`; its `describe()` + `describe_<type>()` methods moved onto `OrderActivityManager`, exposed on the `Activity` facade as `Activity::describe($activity)`. `OrderActivityResource` now calls `Activity::describe($this->resource)` instead of importing the standalone class — one facade for every order-activity concern, recording and describing alike.
- [x] 8.7 Deleted `app/Resources/Site/Order/OrderActivityResource.php` (an empty-body subclass adding nothing). Both controllers now import `App\Resources\Order\OrderActivityResource` directly.
- [x] 8.8 Updated `tests/Integration/OrderActivityApiTest.php`'s comment-creation requests from `orders/{id}/activities/comments` to `orders/{id}/activities`, matching the simplified route. No other test changes needed — assertions target response shape, not internal class names.
- [x] 8.9 Verify: `php -l` on all 20 touched/new files (clean), then re-ran the full `wp eval-file` smoke script end-to-end against the live dev stack — all 17 checks still pass through the new controllers/service/manager, confirming the refactor is behavior-preserving. Dev DB left clean afterward.

## 9. Second post-review refactor

Follow-up review (see design.md's "Second correction during implementation") on naming and a redundant method. No spec/behavior change.

- [x] 9.1 Renamed "actor" → "author" throughout: `OrderActivityManager::resolve_actor()` → `resolve_author()`, its `$actor`/`$actor_name` locals → `$author_id`/`$author_name`, the `order_placed` metadata key `actor_name` → `author_name`; `OrderActivityResource`'s output field `actor_name` → `author_name` and its `resolve_actor_name()` → `resolve_author_name()`. The `created_by` DB column/field is untouched (matches the `orders`/`refunds` convention).
- [x] 9.2 Renamed the facade `app/Facades/Activity.php` → `app/Facades/OrderActivity.php` (class `Activity` → `OrderActivity`), matching the `Order`/`OrderManager`, `Money`/`MoneyManager` naming precedent. Updated the three call sites: `CreateOrderAction.php` (`Activity::order_placed()` → `OrderActivity::order_placed()`), `Http/Controllers/Api/OrderActivityController.php` (`Activity::comment()` → `OrderActivity::comment()`), `Resources/Order/OrderActivityResource.php` (`Activity::describe()` → `OrderActivity::describe()`). No collision with `App\Models\OrderActivity` — confirmed no file imports both.
- [x] 9.3 Collapsed `OrderActivityService` to one delete method: removed the generic `delete(OrderActivity $activity)` (had no caller outside `delete_comment()` itself); `delete_comment(int $order_id, int $id)` now does `find_or_fail` + comment-type guard + `$activity->delete()` inline and is the service's only delete method.
- [x] 9.4 Verify: `php -l` on all 7 touched files (clean); repo-wide grep confirmed zero remaining references to `actor`/`resolve_actor`/`Facades\Activity`. Updated and re-ran the `wp eval-file` smoke script (added an `author_name` assertion) against the live dev stack — 18/18 checks pass through the renamed facade/manager/service. Dev DB left clean afterward.

## 10. Bruno API docs

- [x] 10.1 Added `docs/ecommerce/orders/list-activities.yml` (`GET /orders/{order_id}/activities`, seq 11), `docs/ecommerce/orders/add-comment.yml` (`POST /orders/{order_id}/activities`, seq 12), `docs/ecommerce/orders/delete-comment.yml` (`DELETE /orders/{order_id}/activities/{id}`, seq 13) — matching the existing `orders/` folder's request shape (`info`/`http`/`settings`/`examples`/`docs`), tag `orders`, `auth: inherit`. Examples cover the success case plus the same failure modes exercised in `OrderActivityApiTest.php`/the smoke script (validation 422, wrong-type-delete 422, not-found 404).
- [x] 10.2 Added `docs/ecommerce/account/order-activities.yml` (`GET /account/orders/{id}/activities`, seq 4, tag `account`) — same shape, documenting the ownership check and the "customers see everything, comments included" behavior.
- [x] 10.3 Verify: all four files parse as valid YAML (`python3 -c "yaml.safe_load(...)"`), and `seq` values don't collide with any existing file in `orders/`/`account/`.

## 11. Pagination

Response-shape simplification (`activity_type_label`/`metadata` dropped from `OrderActivityResource`, `order_placed()`'s draft-order description branch removed) landed independently between review rounds — see design.md's "Third correction during implementation". This group covers adding pagination to the list endpoints on top of that current state.

- [x] 11.1 Added `OrderActivityService::paginated_for_order(int $order_id, ListFilterDTO $filters)` and `all_for_order(int $order_id)`, both built on a shared protected `list_query(int $order_id)`, mirroring `BrandService::paginated()`/`all()` exactly (same `ListFilterDTO` + `Pagination::LIMIT`/`Pagination::ALL` convention already used by `OrderController`/`BrandController` and most other list endpoints). Removed the now-superseded plain `paginated_for_order(int, ?int, ?int)` draft.
- [x] 11.2 Updated `Api\OrderActivityController::get()` and `Site\OrderActivityController::get()` to build a `ListFilterDTO` from the request, branch on `Pagination::ALL` (wrapping `all_for_order()`'s result in a `Paginator` for a single full page, same as `BrandController::get()`), and otherwise call `paginated_for_order()`; both now respond with `OrderActivityResource::paginated($data)` instead of `::collection($data)`.
- [x] 11.3 Updated `tests/Integration/OrderActivityApiTest.php`: every `$payload['data']` list read became `$payload['data']['results']`, and added `test_list_activities_is_paginated` asserting `results`/`total`/`per_page`/`current_page`/`last_page`/`has_more_pages` behave correctly across two pages with no id overlap.
- [x] 11.4 Updated `docs/ecommerce/orders/list-activities.yml` and `docs/ecommerce/account/order-activities.yml` with `page`/`limit` query params and the paginated response envelope; also brought `add-comment.yml`/`delete-comment.yml` current (dropped the stale `activity_type_label`/`metadata` fields from their examples, normalized every example URL to the same order id as the top-level request).
- [x] 11.5 Verify: `php -l` clean on all touched files; repo-wide grep confirmed zero remaining references to `list_for_order`. `bash kirki-test integration --filter=OrderActivityApiTest` still blocked by the pre-existing `migrate:fresh` bug (see 3.7) — re-verified live via an updated `wp eval-file` smoke script covering the empty-list case, page 1/3 slicing, `per_page`/`last_page`/`has_more_pages` correctness, no cross-page id overlap, and `limit=-1` returning everything in one page — 26/26 checks passed against the dev stack. Confirmed the 2 leftover rows spotted on order #4 belong to the user's own manual Bruno testing (not this script); my own order #1 test data was fully cleaned up (0 rows) after the run.
