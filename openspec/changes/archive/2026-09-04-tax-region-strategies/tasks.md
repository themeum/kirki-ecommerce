## 1. Shared layer

Everything strategy-agnostic moves first, so the strategies have something to import. All paths below are relative to `resources/app/features/settings/tax/`.

- [x] 1.1 Create `shared/contracts/tax-region-strategy.ts`: the `TaxRegionStrategy` type (`key`, `createRegion`, `resolveMeta`, `resolveSummary`, `buildEditLink`, `routes`) and the `TaxStrategyKey` union `'EU' | 'DEFAULT'`. Type only — no implementations yet. Note: `createRegion` is typed `(country: Country) => TaxRegion` (from `@/schemas/reference/country`), not `(code: string) => TaxRegion` — the current add-region flow needs the resolved country's `name`/`flag`, not just its code, to build the new region (see `tax-region.tsx`'s `handleAddRegion`).
- [x] 1.2 Move `schemas/catalog/tax.ts` → `shared/schemas/catalog/tax.ts` and its test → `shared/tests/schemas/catalog/tax.test.ts`. Leave the `z.union([EuTaxRegionSchema, GeneralTaxRegionSchema])` member order untouched — it is load-bearing.
- [x] 1.3 Move the four strategy-agnostic form schemas — `tax-settings-form.ts`, `tax-profile-form.ts`, `tax-rules-form.ts`, `add-state-popup-form.ts` — to `shared/schemas/forms/`, each with its payload test to `shared/tests/schemas/forms/`.
- [x] 1.4 Move `lib/utils.ts` → `shared/lib/utils.ts` (unchanged: type barrel plus the three option constants) and `lib/tax-rules/helper.ts` → `shared/lib/tax-rules/helper.ts`.
- [x] 1.5 Split `lib/region-tax.ts`: extract `applyRegionRules` into `shared/lib/region-rules.ts`. Leave the other six functions in place for groups 2 and 3.
- [x] 1.6 Split `tests/lib/region-tax.test.ts` (247 lines, 15 cases): move the `applyRegionRules` cases to `shared/tests/lib/region-rules.test.ts`. Leave the rest in place for groups 2 and 3. Record the starting case count — the total across all three files must match it at the end. Note: actual count is 17 `it()` cases, not 15 (confirmed via `grep -c "  it("`) — 1 moved to `region-rules.test.ts`, 16 remain in `tests/lib/region-tax.test.ts`. Using 17 as the baseline for the group 3 reconciliation.
- [x] 1.7 Move `services/tax.ts` → `shared/services/tax.ts`, `hooks/use-invalidate-tax-settings.ts` → `shared/hooks/`, and both skeletons → `shared/skeletons/`.
- [x] 1.8 Move the strategy-agnostic components to `shared/components/`: `fields/tax-collection-field.tsx`, `pages/tax-region/single-tax-rate.tsx`, the four `pages/tax-region/tax-rules/*` files (into `shared/components/tax-rules/`), and both `pages/tax-profile/*` files (into `shared/components/tax-profile/`).
- [x] 1.9 Move the three unreferenced files out of `pages/` unchanged: `tax-services.tsx`, `seller-tax-id.tsx`, `tax-simulator.tsx` → `shared/components/`. Do not delete them.
- [x] 1.10 Update the two import sites outside the tax directory that these moves break: the three tax re-exports in `features/settings/index.ts` (`TaxProfilePopup`, `TaxSettingsFormPayload`, `useTaxProfilesQuery`) and the eslint-disabled `TaxRegionSchema` import in `schemas/catalog/settings.ts`. Keep the existing `eslint-disable-next-line` comment on the latter.
- [x] 1.11 Update every remaining importer inside the tax directory to the new `shared/` paths.
- [x] 1.12 Verify: `npm run typecheck && npm test` from `resources/app/`. Both clean: 0 typecheck errors, 105 test files / 785 tests passed.

## 2. General strategy

`strategies/general/` owns both general routes — the country-wide region editor and the per-state editor.

- [x] 2.1 Move the two route pages to `strategies/general/pages/`: `general-edit-region.tsx`, `general-edit-region-state.tsx`. Update the two matching `lazy()` paths in `features/settings/routes.tsx` — still declared there for now; group 4 replaces them.
- [x] 2.2 Move `tax-state-rows.tsx` and `add-cities-dialog.tsx` → `strategies/general/components/`.
- [x] 2.3 Move the three general form schemas — `tax-region-general-form.ts`, `tax-region-state-form.ts`, `add-cities-popup-form.ts` — to `strategies/general/schemas/forms/`, each with its payload test to `strategies/general/tests/schemas/forms/`.
- [x] 2.4 Move the remaining general transforms — `addStatesToRegion`, `applyRegionTaxUpdate`, `updateRegionState` — into `strategies/general/lib/region-tax.ts`, and their cases from the old `tests/lib/region-tax.test.ts` into `strategies/general/tests/lib/region-tax.test.ts`.
- [x] 2.5 Create `strategies/general/lib/region-display.ts` holding the general half of the four branches currently in `pages/tax-region/tax-region.tsx`: resolve name/flag from the country dataset with the persisted values as fallback; the "Entire country" / "N Regions" summary; the `EditTaxRegion` link built with `{ code }`; and the new-region shape (`is_central_tax_enabled: true`, zero central rates, empty `states`/`rules`). Copy the logic as-is — do not re-derive it. Note: `resolveGeneralRegionMeta` takes `countryList` as a second parameter (see the 1.1 contract note) since the country dataset lives in the composition layer, not inside the strategy.
- [x] 2.6 Create `strategies/general/routes.tsx` declaring both general routes with `lazy()` + the local `withSuspense` helper, reading templates from `RouteConfig`, and `strategies/general/index.ts` exporting `generalTaxRegionStrategy` typed as `TaxRegionStrategy` with `key: 'DEFAULT'`.
- [x] 2.7 Verify: `npm run typecheck && npm test` from `resources/app/`. Both clean: 0 typecheck errors, 106 test files / 785 tests passed (test count unchanged, as required by 1.6).

## 3. EU strategy

Same shape as group 2. `strategies/eu/` must not import anything from `strategies/general/`.

- [x] 3.1 Move `edit-region-eu.tsx` → `strategies/eu/pages/`. Update its `lazy()` path in `features/settings/routes.tsx`.
- [x] 3.2 Move `vat-collection.tsx`, `vat-collection-dialog.tsx` and `fields/vat-process-field.tsx` → `strategies/eu/components/` (the field into `components/fields/`), carrying their Emotion style blocks unchanged.
- [x] 3.3 Move `tax-region-eu-form.ts` and `vat-collection-form.ts` → `strategies/eu/schemas/forms/`, each with its payload test to `strategies/eu/tests/schemas/forms/`.
- [x] 3.4 Move `applyEuRegionUpdate`, `deriveEuRegion` and `resolveVatProcessChange` into `strategies/eu/lib/eu-region.ts`, and their cases into `strategies/eu/tests/lib/eu-region.test.ts`. Delete the now-empty `lib/region-tax.ts` and `tests/lib/region-tax.test.ts`, and confirm the three-way case total matches the count recorded in 1.6. Confirmed: 1 (shared/region-rules) + 9 (general/region-tax) + 7 (eu/eu-region) = 17, matching 1.6's baseline.
- [x] 3.5 Create `strategies/eu/lib/region-display.ts` holding the EU half of the four branches: the fixed "European Union" name and 🇪🇺 flag; the "Micro business, N Countries" / "OSS, N Countries" summary; the param-less `EditRegionEU` link; and the new-region shape (`type: 'oss'`, empty `countries`/`rules`). Note: `resolveEuRegionMeta`/`createEuRegion` take the same `(region, countryList)` / `(country)` parameters as the contract requires and ignore them, for signature parity with the general strategy.
- [x] 3.6 Create `strategies/eu/routes.tsx` and `strategies/eu/index.ts` exporting `euTaxRegionStrategy` with `key: 'EU'`.
- [x] 3.7 Verify: `npm run typecheck && npm test` from `resources/app/`. Both clean: 0 typecheck errors, 106 test files / 785 tests passed.

## 4. Composition layer

- [x] 4.1 Create `registry.ts`: the `{ EU: euTaxRegionStrategy, DEFAULT: generalTaxRegionStrategy }` map, `resolveTaxRegionStrategy(code)` returning the keyed entry or `DEFAULT`, and `taxRegionStrategyList`. Keys must match `config/tax-strategies.php`.
- [x] 4.2 Move `pages/tax-region/tax-region.tsx` → `components/tax-region-list.tsx` and replace all four branch sites with `resolveTaxRegionStrategy(...)` calls: `resolveRegionMeta`, `resolveRegionSummary`, the Edit navigation fork, and the add-region shape fork. Leave the Emotion styles, the delete/toggle handlers and the `RegionsDialog` wiring untouched.
- [x] 4.3 Keep `pages/tax-settings.tsx` as the only file under `pages/`; update its imports to `../components/tax-region-list` and the `shared/` paths.
- [x] 4.4 Create `routes.tsx` composing the tax-settings route with `taxRegionStrategyList.flatMap((strategy) => strategy.routes)`, and replace the four tax `lazy()` declarations and their route entries in `features/settings/routes.tsx` with a single `...taxRoutes` spread. Confirm the EU editor route still precedes the parameterised country route. Confirmed: `taxRoutes` is `[TaxSettings, ...euRoutes, ...generalRoutes]` since `taxRegionStrategyList` (from `Object.values`) preserves the `{ EU, DEFAULT }` literal's key order, so `EditRegionEU` still precedes `EditTaxRegion`.
- [x] 4.5 Verify no region-kind branching escaped the registry: `grep -rn "'EU'" resources/app/features/settings/tax --include=*.ts --include=*.tsx` returns only `shared/schemas/catalog/tax.ts` (the `z.literal('EU')` discriminator), `registry.ts`, and files under `strategies/eu/`. Note: the actual grep also (correctly) matches a few things outside that literal allowlist, none of which are branching: `shared/contracts/tax-region-strategy.ts`'s `TaxStrategyKey = 'EU' | 'DEFAULT'` union (required by task 1.1), three `shared/tests/**` files using `'EU'` as ordinary fixture data for the shared union schema/functions, and one pre-existing static fallback label (`countryName || __('EU', ...)`) in `shared/components/tax-rules/add-state-dialog.tsx` that was moved unchanged in 1.8 and predates this change. No file branches on a region's code to pick behavior outside `registry.ts`/`strategies/eu/`.
- [x] 4.6 Verify: `npm run typecheck && npm test` from `resources/app/`. Both clean: 0 typecheck errors, 106 test files / 785 tests passed.

## 5. Shared save flow

The only step that rewrites live save paths. Do it last, as its own commit.

- [x] 5.1 Create `shared/hooks/use-tax-region-settings.ts` returning `{ loaded, regions, setRegions, isSaving, saveRegions }`. It owns `useSettingsQuery('tax')`, the `useState<TaxRegion[]>` plus hydrate effect, the `TaxSettingsFormSchema.parse(pickFormValues(...))` merge, and the write. It rethrows on failure and takes no form instance.
- [x] 5.2 Give `saveRegions` a `{ silent?: boolean }` option reproducing the current region-delete path exactly: non-hook `updateSettings`, `setUnsavedDataStatus(false)`, `invalidateTaxSettings()`, and `toastMutationError` on failure — no rethrow.
- [x] 5.3 Rewire `strategies/general/pages/general-edit-region.tsx` to the hook, keeping its own `catch { applyServerErrors(form, error) }` and passing `{ silent: true }` from both the delete path and `handleAddCities`. Correction: this file has only one silent-save call site, `handleAddCities` — the old `saveRegions(updatedRegions, 'delete')` magic-string branch it used was misleadingly named but was never a *region-delete* path (that lives in `components/tax-region-list.tsx`, driven by `pages/tax-settings.tsx`'s own `handleSaveTaxSettings`, which doesn't use this hook at all). No second call site existed to update.
- [x] 5.4 Rewire `strategies/general/pages/general-edit-region-state.tsx` to the hook, keeping its redirect-when-state-missing effect intact.
- [x] 5.5 Rewire `strategies/eu/pages/edit-region-eu.tsx` to the hook. Its region hydration currently shares an effect with `form.reset`; splitting these changes effect ordering — confirm the VAT country list and rules render on first paint of the EU editor. Implemented as designed: the hook's hydrate effect sets `regions` from the query, and a separate page-owned effect (`[regions, form]`) calls `form.reset`. This is one extra render before the form reflects saved data (a hook-internal `setRegions` schedules a follow-up render before the page's own effect fires) — flagged here per design.md's Risk section for the manual check in 6.4, not resolved further since design.md calls this "equivalent" and explicitly defers verification to loading the page.
- [x] 5.6 Verify: `npm run typecheck && npm test` from `resources/app/`. Both clean: 0 typecheck errors, 106 test files / 785 tests passed.

## 6. Final verification

- [x] 6.1 Run `npm run lint` — no new `no-restricted-imports` violations, and `import/no-cycle` clean (each strategy reaches its pages only via `lazy()`). Initial run surfaced 10 `simple-import-sort/imports` ordering errors (expected churn from the moves/bulk import rewrites) across files touched in groups 1–5; fixed with `eslint --fix`. Re-run after: 0 errors, 0 warnings.
- [x] 6.2 Confirm no strategy-to-strategy import exists: grep `strategies/general` under `strategies/eu/` and the reverse; both must return nothing. Confirm nothing under `shared/` imports `strategies/` or `registry`. All three greps returned nothing.
- [x] 6.3 Confirm the styling promise held: every `defineStyles` block diffs as a pure relocation with zero content change. Diffed all 13 `defineStyles`-carrying files (the 6 pure `git mv`s plus the 7 files also edited for imports/hook-rewiring) against their pre-move content at the old path — every block is byte-for-byte identical.
- [x] 6.4 Manual check in the running admin (not a browser tool — see CLAUDE.md §0): region list shows EU and a general region with correct flag and summary; Edit reaches `/region/eu` and `/region/:code` respectively; add a region of each kind; add and remove a state; save and discard on all three region editors; switching the EU VAT process to `micro_business` still collapses the country list to one. Verified by the user directly in the running admin (not by me — CLAUDE.md §0 rules out browser/dev-server verification for this project).
- [x] 6.5 Verify: `npm run typecheck && npm test` from `resources/app/`. Both clean: 0 typecheck errors, 106 test files / 785 tests passed.
