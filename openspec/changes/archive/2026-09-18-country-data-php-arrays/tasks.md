## 1. Lock in the safety net first

- [x] 1.1 Add `tests/Unit/Supports/CountryDataIdentityTest.php` asserting `assertSame()` between `Utils::get_countries()` and `json_decode(file_get_contents(<source JSON>), true)`. It passes trivially before the migration and becomes the real proof after — write it before touching anything else.
- [x] 1.2 Add a case to the same test asserting the first country's keys are in exact source order (`name, code, phone_code, currency, currency_name, currency_symbol, flag, states, numeric_code, group`), so a key-order regression fails with a readable message rather than a 250-entry array diff.
- [x] 1.3 Run `composer test:unit` and confirm both pass against the current implementation. **Correction:** the Unit suite does not stub `plugin_dir_path`, which `Utils::get_countries()` called, so the test could not run pre-migration at all. Added a temporary `plugin_dir_path` stub to `tests/Support/StubsWordPressFunctions.php` to get the baseline green (4 tests / 5,245 assertions) before any production code changed. Once `Utils` delegated to `CountryData` the stub was unreachable, so it was removed again in 8.5 — that file ends with no diff.

## 2. Relocate the source and build the generator

- [x] 2.1 `git mv resources/data/countries.json data-src/countries.json`.
- [x] 2.2 Point the test from 1.1 at `data-src/countries.json`; confirm it still passes (nothing else reads the file yet — `CountryService` and `Utils` still will, so fix their paths temporarily or expect them to break until section 4).
- [x] 2.3 Write `bin/generate-country-data.php`: reads `data-src/countries.json`, emits `resources/data/countries.php` (keyed by ISO code, fields in source order minus `states`, plus `has_states`) and `resources/data/states.php` (keyed by country code, entries `['id' => <numeric>, 'name' => <name>]`, absent for the 50 countries with no states).
- [x] 2.4 ~~Derive the ISO 3166-2 code for each state.~~ **Dropped.** The source holds only `id` and `name` for all 4,991 states and an ISO code cannot be derived from a name, so the field would have been null everywhere. Confirmed with the user; spec requirement amended and the stable-identifier problem deferred to a future dataset re-import.
- [x] 2.5 Generated files must open with `<?php` and `return [...]`, use short array syntax, and carry a header comment stating they are generated by `bin/generate-country-data.php` and must not be hand-edited.
- [x] 2.6 Run the generator; confirm `countries.php` has 250 entries and `states.php` has 200.

## 3. Build the loader

- [x] 3.1 Add `app/Supports/CountryData.php` with `index()`, `states_for(string $code)` and `nested()`. All loading lazy — `require` inside the method on first call, cached in a protected static. Nothing at file scope.
- [x] 3.2 `nested()` reassembles the public list: write the ten keys in source order, splice `states` into position 8 (between `flag` and `numeric_code`), drop `has_states`, then `array_values()`. Memoize the result separately from the index.
- [x] 3.3 Follow project conventions: `protected` not `private`, `static::` not `self::`, snake_case methods, PHP 7.4 syntax only.

## 4. Migrate the consumers

- [x] 4.1 `app/Supports/Utils.php:357` — `get_countries()` delegates to `CountryData::nested()`. Remove the `file_get_contents` + `json_decode`.
- [x] 4.2 `app/Services/CountryService.php` — `load_data()` reads from `CountryData`; `find_by_code()` becomes an O(1) keyed lookup on the index (still returning the country with its states nested, and still case-insensitive on the code).
- [x] 4.3 `app/Services/CurrencySwitcherService.php:148` — read `CountryData::index()` instead of the full nested dataset, so flag resolution stops loading 4,991 states.
- [x] 4.4 Run `composer test:unit`. Test 1.1 must pass. If it fails, the reassembly is wrong — fix it before continuing.

## 5. Singleton binding and the `all()` fix (must land together)

- [x] 5.1 Fix `CountryService::all()`: filter into a local variable instead of reassigning `$this->data`, and `array_values()` the filtered result so a group-filtered response serializes as a JSON array.
- [x] 5.2 Add a unit test: read with `group = 'eu'`, assert the result is sequentially indexed (`array_keys(...) === range(0, count - 1)`) and contains 27 countries; then read again with no filter and assert all 250 come back.
- [x] 5.3 Bind `CountryService` as a singleton in the appropriate service provider (`AppServiceProvider`). Confirm 5.2 still passes — that test is what proves the binding is safe. **Note:** the Unit bootstrap does not register service providers, so the binding itself cannot be asserted there; the leak-guard test in 5.2 proves the dangerous property is fixed regardless of binding, and the Integration suite exercises the real container.
- [x] 5.4 Verify `app/Resources/Site/Order/OrderResource.php:129-130` now resolves one shared instance rather than constructing per call. Proven by `tests/Integration/CountryServiceBindingTest.php`, which asserts container identity and that a group-filtered read does not narrow the shared instance for later consumers.

## 6. Verify the untouched surfaces

- [x] 6.1 Confirm no TypeScript changed: `git diff --stat -- resources/app resources/site` must be empty.
- [x] 6.2 Confirm the storefront views need no edits — `resources/views/site/checkout/parts/{shipping,billing}-form.php` and `resources/views/site/account/parts/{address-form,address-modal}.php` iterate `$countries` in the preserved shape.
- [x] 6.3 Confirm `app/Hooks/Filters/PageInlineScript.php:86,141`, `SiteController.php:217` and `AccountController.php:198` still emit the same config — the inline payload is deliberately unchanged in this change.
- [x] 6.4 Confirm `CountryListResource` / `CountryResource` were not modified.
- [x] 6.5 Confirm `resources/data/{currencies,european_union_countries,order-state-matrix}.json` and `resources/data/settings/` are untouched, and that `EuropeanCountryChecker.php:27` still resolves.

## 7. Packaging and CI

- [x] 7.1 Confirm `bin/make-package.sh` needs no edit — `data-src/` is outside its copied paths. **Verified statically rather than by a full build:** the script copies only the explicit `REQUIRED_PATHS`/`OPTIONAL_PATHS` entries, neither of which contains `data-src`, and `resources/data/` (copied wholesale) no longer holds any `countries.json`. A full `make:package` run does a `composer update` in a staging dir, which is slow and network-dependent; the path list is dispositive without it.
- [x] 7.2 Add a `country-data-drift` job to `.github/workflows/tests.yml`: run `bin/generate-country-data.php`, then `git diff --exit-code resources/data/countries.php resources/data/states.php`.

## 8. Final gates

- [x] 8.1 `composer test` (Unit + Integration) passes. Unit: 306 tests / 5,811 assertions. Integration: 452 tests / 9,773 assertions (run in Docker; the WordPress test library is not installed on the host).
- [x] 8.2 `composer phpcs:wporg` passes.
- [x] 8.3 `npm run typecheck:app` passes.
- [x] 8.4 `npm test` in `resources/app/` passes unchanged.
- [x] 8.5 Re-read the final diff and confirm every changed line traces to this change — no drive-by edits to adjacent code. Removed the temporary `plugin_dir_path` stub from 1.3 once it was orphaned, leaving `tests/Support/StubsWordPressFunctions.php` untouched.
