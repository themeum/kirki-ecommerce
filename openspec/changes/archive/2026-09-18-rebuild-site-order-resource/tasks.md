## 1. Fix shipping tax to always be additive (tax-calculation-strategy)

- [x] 1.1 Add `calculate_shipping_tax_amount(float $rate, int $base_amount): int` to `app/Tax/Strategies/AbstractTaxStrategy.php` — always the added-on-top computation, ignoring `$this->is_tax_inclusive_price`. Leave `calculate_tax_amount()` untouched for product lines.
- [x] 1.2 Update `DefaultTaxStrategy::calculate_shipping_tax()` to build its `TaxLineDTO` amount via `calculate_shipping_tax_amount()`.
- [x] 1.3 Update `EUTaxStrategy::calculate_shipping_tax()` — both the per-item-allocated-portion branch and the no-items fallback branch — to use `calculate_shipping_tax_amount()`.
- [x] 1.4 Update `RecalculateCartAction::build_shipping_result()` to drop the `$is_inclusive_tax` parameter and always `$total_money->plus($tax_money)`; update its call site in `execute()` accordingly.
- [x] 1.5 Update/add unit tests in `tests/Unit/Tax/DefaultTaxStrategyTest.php` and `tests/Unit/Tax/EUTaxStrategyTest.php` covering: tax-inclusive-price setting on + taxable shipping → shipping tax line is still additive, not extracted.
- [x] 1.6 Update/add unit tests in `tests/Unit/Actions/Cart/RecalculateCartActionTest.php` covering: tax-inclusive-price setting on + taxable shipping → shipping tax is added on top of `base_shipping_total`/`base_total`, matching the new `tax-calculation-strategy` scenarios.
- [x] 1.7 Verify: `composer test:unit` and `composer phpcs:wporg` both pass. (Also fixed a pre-existing test-fixture gap in `tests/Support/BindsTaxDependencies.php` — its fake settings factory didn't implement the real `SettingsFactory`'s dot-notation group split, so every `RecalculateCartActionTest` errored before this change too; confirmed via `git stash` against unmodified code. `composer test:unit` still has 7 unrelated pre-existing errors in `UnitPriceTest.php`, untouched by this change.)

## 2. Rewrite the storefront order resource (storefront-order-pricing-breakdown)

- [x] 2.1 Rewrite `app/Resources/Site/Order/OrderResource.php` to stop extending `app/Resources/Order/OrderResource.php` — build `to_array()` from the order's own persisted relations (`items`, `order_coupons`, `order_item_coupons` via each order-coupon, `order_taxes`/`shipping_taxes`), `invoiced_*`-only. (Also dropped `admin_notes` from the payload — it was previously leaking through via `array_merge(parent::to_array(), ...)`; internal staff notes have no reason to reach a customer-facing order-details page. Flagged to the user, not silent.)
- [x] 2.2 Implement the root `pricing` block: `invoiced_items_subtotal`, `invoiced_order_discount`, `invoiced_order_total`, `invoiced_tax_total`, `coupons[]`, `invoiced_shipping_amount`, `invoiced_shipping_strikethrough` (per design Decision 4), `invoiced_total`, merged `tax_lines[]` — every field as `*_money_object` only (design Decision 2), built with `Money::prepare_amount_object_from_minor($amount, $this->currency_code)`. (Caught and fixed a swapped-label bug in Decision 3's original wording while implementing this — see design.md's "Correction during implementation".)
- [x] 2.3 Implement per-item output: `invoiced_subtotal` (net of only that item's product-scoped coupon share), `invoiced_strikethrough_price`, `applied_product_coupons[]`, item tax lines — mirroring `CartResource::prepare_items()`'s shape and helper split (product-coupon-discount-for-item vs. order-coupon-discount). (Strikethrough only covers the coupon case, not the sale-price case — see design.md Decision 7, a real persisted-data gap, not an oversight.)
- [x] 2.4 Implement `coupons[]` formatting from `order_coupons` rows: `code`, `title`, `discount_type`, `discount_target`, and `discount_value_type`/`discount_amount_percentage`/`discount_amount_fixed` read from `coupon_snapshot`, plus `invoiced_discount_amount_money_object`. (`discount_amount_fixed` converted to invoiced currency via the order's own frozen `exchange_rate`, not a live rate lookup — see `convert_base_amount_to_invoiced()`.)
- [x] 2.5 Implement the shared tax-line grouping helper (by name+rate, dropping zero-amount lines) reused for both the root merged `tax_lines` and per-item tax lines — port `CartResource::format_tax_breakdown()`'s logic to `invoiced_amount_money_object`.
- [x] 2.6 Re-fold the existing storefront-only fields into the new class unchanged: `shipping_country`, `billing_country`, `formatted_status`, `payment_next_step` (`resolve_payment_next_step()` carried over as-is), `item_product_data`, `customer`.
- [x] 2.7 Confirm pre-existing orders with no `order_coupons` rows produce an empty `coupons` list, zero `invoiced_order_discount`, and unmodified per-item `invoiced_subtotal` — no special-case code required; verified in the new test suite (task 2.8) with an explicit no-coupons case, plus every helper guards a possibly-null relation with `?: collection()`.
- [x] 2.8 Add/update unit tests for the new resource. Written at `tests/Unit/Resources/OrderResourceCouponFormattingTest.php` (flat, matching the existing `CartResourceCouponFormattingTest.php`/`OrderCalculationResourceCouponFormattingTest.php` convention in that directory, not the nested path originally sketched in this task). Covers: single item-scoped coupon, single order-scoped coupon, multiple coupons mixing both scopes, a free-shipping order-coupon (shipping discount derived correctly with zero item attributions), an order with no order-coupons at all, and the snapshot fixed-amount currency conversion. 18 tests, all passing.
- [x] 2.9 Verify: `composer test:unit` and `composer phpcs:wporg` both pass. (Full suite: 296 tests, same 7 pre-existing unrelated `UnitPriceTest` errors as before this change, 0 new failures. phpcs:wporg: 0 errors, only pre-existing unrelated warnings.)

## 3. Cross-check against the admin resource and cart parity

- [x] 3.1 Confirm `app/Resources/Order/OrderResource.php` (admin) and its consumers are untouched — no shared base class, no accidental behavior change. (`git diff --stat -- app/Resources/Order/OrderResource.php` and `git status --short app/Resources/Order/` both empty.)
- [x] 3.2 Manually diff the new storefront payload's `pricing`/`items`/`coupons` field names against `CartResource`'s equivalent blocks.
  - `pricing`: same key set and structure (`items_subtotal`, `order_discount`, `order_total`, `tax_total`, `coupons`, `shipping_amount`, `shipping_strikethrough`, `total`, `tax_lines`), every field renamed `display_*_money_object` → `invoiced_*_money_object` (deliberate, per Decision 2 — an order has no display-currency conversion step).
  - `coupons[]`: same fields, minus Cart's bare `base_discount_amount_fixed` int (deliberately dropped — no `base_*` output) and with `display_discount_amount_fixed_money_object` → `invoiced_discount_amount_fixed_money_object` (converted via the order's frozen `exchange_rate`, not a live lookup — see `convert_base_amount_to_invoiced()`).
  - `items[]`: the coupon/subtotal/strikethrough shape matches Cart 1:1 (`invoiced_subtotal_money_object`, `invoiced_strikethrough_price_money_object`, `applied_product_coupons`), but the product-identity fields are deliberately *not* Cart's nested `product` object (`variant`/`media`/`categories`/`attributes`/live stock flags) — an order item has no live product/variant relation to trust after checkout, only its own frozen `product_name`/`variant_name`/`sku`/`product_image` columns, matching what admin's `OrderResource` already reads for the same reason. Also, per design.md Decision 7, the strikethrough only covers the coupon case, not Cart's sale-price case (no persisted regular-price column on order items).
- [x] 3.3 Verify: `composer test:unit` and `composer phpcs:wporg` both pass for the full changed set. (296 tests, 7 pre-existing unrelated errors, 0 phpcs errors — same as task 2.9's run.)
