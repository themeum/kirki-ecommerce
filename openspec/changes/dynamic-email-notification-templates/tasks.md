## 1. Backend: add per-key order-event Mail classes

- [x] 1.1 Add `app/Mails/customers/CustomerOrderProcessingMail.php`, `CustomerOrderOnHoldMail.php`, `CustomerOrderCompletedMail.php`, `CustomerOrderRefundedMail.php`, `CustomerOrderCancelledMail.php`, `CustomerOrderFailedMail.php` — each a copy of `CustomerOrderConfirmationMail.php`'s `with()` logic, with `option_key()` hardcoded to its own `customer_emails.order_notifications.<key>` path. `CustomerOrderConfirmationMail.php` itself is unchanged.
- [x] 1.2 Add `app/Mails/admins/AdminOrderConfirmationMail.php`, `AdminOrderProcessingMail.php`, `AdminOrderOnHoldMail.php`, `AdminOrderCompletedMail.php`, `AdminOrderRefundedMail.php`, `AdminOrderCancelledMail.php`, `AdminOrderFailedMail.php` (7 classes) — same `with()` logic, `option_key()` hardcoded to `admin_emails.order_notifications.<key>`.
- [x] 1.3 Verify: `composer phpcs:wporg`; `npm run typecheck && npm test` (from `resources/app/`). phpcs clean on new files. Pre-existing, unrelated failures noted (not caused by this change): 1 typecheck error (`info_cards`/`info_cads` typo in `email/tests/lib/template.test.ts`) and 4 test failures (`EmailSettingsSchema`/`getSettings('email')` — traced to `default_template: EmailTemplateShape` being non-nullish, from earlier commits `7e4a532d`/`4166e647`, unrelated to `EmailNotificationSchema`).

## 2. Backend: content overrides + new Mailer classes + sample fixtures

- [x] 2.1 Add `$content_overrides`, `with_content_overrides(array $overrides)`, and `get_option_value(string $suffix, $default = '')` to `app/Mails/Mailer.php`, mirroring the existing `template_overrides`/`with_template_overrides()`/`get_default_template()` pattern; switch `subject()` and `get_variables()` to read `subject`/`heading`/`message` via `get_option_value()` instead of `Settings::get('email')->get(...)` directly.
- [x] 2.2 Inspect `app/Models/Customer.php` and add `resources/data/sample/customer.json`, a fixture-backed sample customer (name, email) for preview use, following the same in-memory/never-persisted pattern as `EmailTemplateController::build_sample_order()`.
- [x] 2.3 Add `app/Mails/ResetPasswordNotificationMail.php`: constructor takes the sample customer data + `$option_key`; `with()` returns `customer_name`, `customer_email`, and a fabricated `reset_url` (no real reset flow exists in this codebase yet — this is preview/test-mail only).
- [x] 2.4 Inspect `app/Models/Product.php`/`app/Models/Variant.php` and add `resources/data/sample/product.json`, a fixture-backed sample product/variant (name, SKU, available quantity) for preview use.
- [x] 2.5 Add `app/Mails/LowStockNotificationMail.php`: constructor takes the sample product data + `$option_key`; `with()` returns product name, SKU, available quantity, and a link to the product edit screen. Also added `Url::get_product_admin_edit_url()` (mirroring the class's other `get_*_url()` helpers) since no admin-URL builder existed yet.
- [x] 2.6 Verify: `composer phpcs:wporg`; `npm run typecheck && npm test`. phpcs clean; `php -l` clean on all new/changed files. No frontend files touched in this group, so the frontend baseline is unchanged from group 1's noted pre-existing failures.

## 3. Backend: registry, dynamic controller endpoints, routes, template fix

- [x] 3.1 Add `app/Mails/EmailNotificationRegistry.php` with `public static function resolve(string $type, string $group, string $key)`, mapping every one of the 17 real `type/group/key` combinations directly to its Mailer class (each order-event combination to its own dedicated `Customer*Mail`/`Admin*Mail` class from Group 1; both `reset_password` combinations to the shared `ResetPasswordNotificationMail`; `admin/inventory/low_stock` to `LowStockNotificationMail`) plus the sample-data builder each needs, returning `null` for anything outside the known set.
- [x] 3.2 **Deviation from original task wording (explicit user direction during implementation):** instead of a separate `SendNotificationTestEmailRequest`, extended the existing `app/Http/Requests/Settings/SendTestEmailRequest.php` with nullable `subject`/`heading`/`message` rules (string/string/rich text via `Sanitizer::RICH_TEXT`) alongside its existing branding fields, since `send_test_mail()` (task 3.4) is no longer a separate method and needs to validate both branding and content overrides in one request class.
- [x] 3.3 **Deviation (explicit user direction):** rather than adding a separate `preview_notification()` method, `EmailTemplateController::preview()` itself was changed to take `(Request $request, string $type, string $group, string $key)` and resolve via the registry (404 with a clear message if unknown), returning `{ data: { html, variables }, message }`. The old flat `/settings/email/preview` behavior (hardcoded to `CustomerOrderConfirmationMail`) is gone — the default-template (branding) editor must now call this same endpoint with fixed segments (`customer/order_notifications/order_confirmation`). **This reverses design.md's Non-Goal** ("No changes to the default-template editor or its existing flat `/settings/email/preview` endpoints") — design.md/proposal.md should be updated to reflect this before archiving.
- [x] 3.4 **Deviation (explicit user direction):** rather than a separate `send_test_notification_mail()` method, `EmailTemplateController::send_test_mail()` itself was changed to take `(SendTestEmailRequest $request, string $type, string $group, string $key)`, resolves via the registry, and calls `->with_template_overrides($request->all())->with_content_overrides($request->all())` (both are safe to call unconditionally — each only reads the specific keys it cares about), sending to `user()->get_email()` with the same error handling as before.
- [x] 3.5 **Deviation (explicit user direction):** the flat `/settings/email/preview` and `/settings/email/preview/test-mail` routes were removed entirely (not kept additively) and replaced with only `GET /settings/email/{type}/{group}/{key}/preview` → `preview` and `POST /settings/email/{type}/{group}/{key}/preview/test-mail` → `send_test_mail` in `routes/api.php`. `EmailTemplateController::build_sample_order()` was removed as dead code (its logic now lives in `EmailNotificationRegistry`).
- [x] 3.6 In `resources/views/emails/layouts/heading.php`: wrapped the "Order {order_number}" eyebrow `<tr>` in `if (!empty($data['order_number']))`; added `heading` to the heading `<p>`'s `data-email-part` attribute (`data-email-part="colors.typography.headings heading"`).
- [x] 3.7 Verify: `composer phpcs:wporg`; `npm run typecheck && npm test`. phpcs clean; `php -l` clean; grep confirms no dangling references to removed methods/classes. Frontend groups (6/7) will need to update the existing default-template editor's service/endpoint to call the new dynamic route with fixed segments — tracked as an added step in those groups rather than a separate task here.

## 4. Frontend: extend the shared notification schema

- [x] 4.1 **Deviation (explicit user direction):** extended `EmailNotificationSchema` with `subject`/`heading`/`message` (`z.string().nullish()`), but *without* `shortcodes` and *removing* the previously-present `name` field — neither is actually persisted by the backend (`resources/data/settings/email.json` entries only ever have `is_enabled`/`subject`/`heading`/`message`; `shortcodes` data that exists there is unreliable placeholder content copy-pasted across notification types, so it's deliberately not modeled — the editor's reference shortcode list will come from the preview endpoint's `variables` instead, see group 7/8). Added a `key: z.string().nullish()` field instead (the entry's own record key, attached when list items are derived), per explicit direction. Comment updated accordingly.
  - **Update (explicit user direction, superseding the above):** `shortcodes: z.array(z.object({ label: z.string(), value: z.string() })).nullish()` was reintroduced onto `EmailNotificationSchema`, matching the actual `email.json` shape (`{label, value}[]`). The editor's shortcode reference list now reads this field directly from the settings query instead of the preview endpoint's `variables` (see 7.5/8.4 updates below). The live preview's `{tag}` substitution is unaffected — it still reads `variables` from the preview endpoint.
- [x] 4.2 Updated `schemas/catalog/settings.test.ts`: added a dedicated `EmailNotificationSchema` describe block, and updated the `EmailSettingsSchema` fixtures to drop `name`/`shortcodes` and use real notification keys.
- [x] 4.3 Updated `EmailNotificationFormShape` in `schemas/forms/email-settings-form.ts`: removed the `name` field (matching 4.1 — it isn't real) and rewrote the comment; updated `email-settings-form.test.ts` fixtures to match.
- [x] 4.4 Verify: `npm run typecheck && npm test`. Same 4 pre-existing unrelated failures as group 1; 2 more tests passing than baseline (the new `EmailNotificationSchema` tests). No new failures from this group's schema changes.

## 5. Frontend: notification key dictionary and list-page fixes

- [x] 5.1 In `resources/app/features/settings/email/lib/utils.ts`, added `NOTIFICATION_TEMPLATES` (canonical list of all 17 real notification templates, `{ type, group: 'order'|'user'|'inventory' (short form, matching the backend registry — see 3.1's fix), key, label }[]`), `resolveNotificationTemplate(item, prefix)`, and `getNotificationTemplateLabel(ref)`. Replaced the hand-rolled `EmailListItem`/`EmailNotification`/`EmailGroupData` types (here, in `customer-email.tsx`/`admin-email.tsx`, and also in `pages/email-settings.tsx`, which had its own copy too) with an exported `EmailListItem = z.infer<typeof EmailNotificationSchema> & { key: string }`.
- [x] 5.2 In `customer-email.tsx` and `admin-email.tsx`, `EmailRow` now takes an explicit `label` prop (computed at each `.map()` call site via `getNotificationTemplateLabel(resolveNotificationTemplate(item, prefix))`) instead of reading `item.name` (which no longer exists on the schema, see 4.1).
- [x] 5.3 In `pages/email-settings.tsx`'s `handleToggleOrder`, replaced the `findEmailKeyByName` lookup with `resolveNotificationTemplate`, which also let the now-dead manual `groupData` lookup be removed (the `!payload` check in `buildTogglePayload` already covers a missing entry).
- [x] 5.4 Verify: `npm run typecheck && npm test`. Both clean — no new failures; the 4 pre-existing `default_template`-related failures from group 1 are unchanged, and the 2 new `EmailNotificationSchema` tests pass.

## 6. Frontend: route, form schema, service

- [x] 6.1 In `resources/app/config/route-config.ts`, added `EditNotificationTemplate: defineRoute('/:type/:group/:key')` under `Settings.EmailSettings`.
- [x] 6.2 In `resources/app/features/settings/routes.tsx`, lazy-imported and registered the new page as a top-level route entry (same placement as `EditEmailTemplate`, outside `SettingsLayout`'s `children`), plus its skeleton import.
- [x] 6.3 Added `schemas/forms/email-notification-template-form.ts` (feature-local path, per the plan's path correction — not top-level `resources/app/schemas/forms/`): `EmailNotificationTemplateFormShape = EmailNotificationSchema.pick({ subject: true, heading: true, message: true })`, wrapped in `prepareFormSchema(...).transform(...)`; added its payload test.
- [x] 6.4 **Deviation (explicit user direction, follows from 3.3-3.5):** since the flat `/settings/email/preview` routes were removed rather than kept, replaced the old `EMAIL_TEMPLATE_PREVIEW`/`EMAIL_TEMPLATE_SEND_TEST_MAIL` string constants in `endpoints.ts` with `EMAIL_NOTIFICATION_PREVIEW(type, group, key)`/`EMAIL_NOTIFICATION_SEND_TEST_MAIL(type, group, key)` builder functions, and updated the existing default-template editor's service (`services/email-template-preview.ts`) to call them with a fixed `customer/order/order_confirmation` target (documented inline) — that's "the default one" per the user's direction.
- [x] 6.5 Added `services/email-notification-template.ts`: `useEmailNotificationPreviewQuery(type, group, key)` (GET, `staleTime: Infinity`, returns `{ html, variables }`) and `useSendNotificationTestEmailMutation()` (POST). Added a matching `emailNotificationPreviewKeys` entry to `libs/query-keys.ts`.
- [x] 6.6 Verify: `npm run typecheck && npm test` — now clean (see 7.8; the transient `TS2307` errors noted above are resolved by group 7's page/skeleton).

## 7. Frontend: editor page (context, hook, layout, skeleton)

- [x] 7.1 Added `buildNotificationTemplatePayload` (plus `notificationRootKey`/`notificationGroupKey` helpers mapping a `NotificationTemplateRef`'s short `type`/`group` to the real `[type]_emails`/`[group]_notifications` settings keys) alongside `buildEmailTemplatePayload` in `lib/template.ts`, rebuilding the full email-settings payload with one leaf replaced.
- [x] 7.2 Added `contexts/edit-notification-template-context.tsx`: reads `type`/`group`/`key` via `useParams()`, loads `useSettingsQuery('email')`, resets the form from the matching leaf record, saves via `buildNotificationTemplatePayload`, reuses `useUnsavedNavigationGuard` exactly as `EditTemplateProvider` does. Also exposes `ref` and a resolved `label` for the layout to use as the page title.
- [x] 7.3 Added `hooks/use-edit-notification-template.ts` (trivial context accessor, mirrors `use-edit-template.ts`).
- [x] 7.4 Added `components/layouts/email-notification-template-layout.tsx` (back button to Email settings, title from the dictionary label via context, Save/Discard), mirroring `email-template-layout.tsx`.
- [x] 7.5 Added `pages/edit-notification-template.tsx`: `TextField name="subject"`, `TextField name="heading"`, `RichTextField name="message"`. **Deferred to group 8:** the read-only shortcodes reference list — per 4.1's deviation, the backend `shortcodes` data isn't modeled/trusted, so the reference list is sourced from the preview endpoint's `variables` instead, which isn't wired up until 8.2-8.4.
  - **Update:** per 4.1's reversal, the reference list now comes from `currentNotification.shortcodes` (exposed via `EditNotificationTemplateContextValue`), not the preview endpoint.
- [x] 7.6 Added `skeletons/edit-notification-template-skeleton.tsx`, mirroring `edit-template-skeleton.tsx`.
- [x] 7.7 In `pages/email-settings.tsx`, replaced `handleEditOrder = noop` with a real handler using `resolveNotificationTemplate(item, matchedConfigKey)` to build `RouteConfig.Settings.get('EmailSettings').get('EditNotificationTemplate').buildLink(template)`.
- [x] 7.8 Verify: `npm run typecheck && npm test`. Both clean (typecheck: zero errors; test: same 4 pre-existing unrelated failures from group 1, 2 more tests passing than baseline from the new form schema test).

## 8. Frontend: real-time shortcode preview

- [x] 8.1 Added `lib/shortcode.ts`: `interpolateShortcodes(content, variables)`, a client-side mirror of `ShortcodeParser`'s `/\{([a-zA-Z0-9_]+)\}/` regex and its exact semantics (nullish/missing → literal text); added its unit test (4 cases).
- [x] 8.2 Added `components/email-notification-template-preview/email-notification-template-preview.tsx`, same iframe/`srcDoc`/`onLoad` pattern as `EmailTemplatePreview`, backed by `useEmailNotificationPreviewQuery`.
- [x] 8.3 Added `hooks/use-email-notification-preview-sync.ts`: on `form.watch(...)` (RHF 7 has no name-filtered subscribe overload — watches all fields and re-applies, same as the existing `use-email-template-preview-sync.ts`), interpolates the draft heading/message against the preview's `variables` and patches `[data-email-part~="heading"]` (`textContent`, added in 3.6) and `[data-email-part~="body"]` (`innerHTML`, already present in `layouts/body.php`) in the iframe document; reuses `use-email-template-preview-auto-height.ts` for iframe height sync. The interpolated subject is rendered as plain text above the iframe, in the preview component itself.
- [x] 8.4 Wired the preview component and Send Test Mail button (`useSendNotificationTestEmailMutation()`) into `pages/edit-notification-template.tsx`. Also moved the read-only shortcodes reference list here (deferred from 7.5) — sourced from the preview query's `variables`, filtered to scalar values only, rendered as `{tag}` badges.
  - **Update:** per 4.1's reversal, this list is no longer derived from `variables` — it's now `EditNotificationTemplateContextValue.shortcodes`, read straight from the settings query's `currentNotification.shortcodes` (`{label, value}[]`, same shape `RichTextField` expects, so it's passed through unmapped). The preview panel itself (this task's iframe/`{tag}`-substitution wiring) is otherwise unchanged.
- [x] 8.5 Verify: `npm run typecheck && npm test`. Both clean — typecheck zero errors; tests show the same 4 pre-existing unrelated failures plus 4 more passing (the new shortcode tests).

## 9. Final verification

- [x] 9.1 Ran `composer phpcs:wporg` clean across all new/changed PHP files (Mail classes, `Mailer.php`, `EmailNotificationRegistry.php`, `Url.php`, `EmailTemplateController.php`, `SendTestEmailRequest.php`, `routes/api.php`, `heading.php`). Also ran `php -l` on every touched file — all clean.
- [x] 9.2 Ran `npm run typecheck && npm test` and `eslint` clean from `resources/app/`. Typecheck: zero errors. Lint: found and fixed 5 real issues (import sort ×2, an unnecessary type assertion in `use-email-notification-preview-sync.ts`, an unsafe `String(value)` on an `unknown` in `lib/shortcode.ts` — fixed by only stringifying string/number/boolean and leaving anything else as literal text, which also hardens against non-scalar `variables` entries, and an unnecessary assertion in `lib/utils.ts`). Test: 1033/1037 passing; the 4 failures are the pre-existing, unrelated `default_template`-required-field issue flagged in task 1.3 — untouched by this change.
- [x] 9.3 Fix `resources/app/components/rich-text.tsx`: the TinyMCE-init `useEffect` (deps: `[id]`) only calls `editor.setContent(value)` once at mount, using whatever `value` prop was present at that render. In both `EditNotificationTemplateProvider` and `EditTemplateProvider`, `loaded: !isLoading` flips synchronously in the same render the settings query resolves, but the corresponding `form.reset(...)` call lives in a separate `useEffect` that only commits after that render — so `RichTextField` mounts `RichText` with the pre-reset (empty) value, and the corrected value applied by `form.reset()` is never pushed into the already-initialized editor. `message`/`heading`/`footer` fields render empty even though `form.watch()` has the correct content. Add a second effect that syncs an externally-changed `value` prop into the live editor after init (e.g. only call `editor.setContent(value || '')` when `editor.getContent() !== (value || '')`, so it doesn't clobber in-progress typing or cursor position). Also remove the stray `console.log('form', form.watch())` left at `edit-notification-template.tsx:41` from diagnosing this. This is a pre-existing defect in the shared `RichText` component — not introduced by this change — and also affects `edit-template.tsx`'s Heading/Footer fields; it surfaced during this change's own testing of the new Message field.
- [ ] 9.4 Ask the user to visually confirm the live shortcode preview and the full edit/save/preview/send-test-mail flow in a browser — per CLAUDE.md §0, no dev-server/browser preview is performed as part of implementing this change.
