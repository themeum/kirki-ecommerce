## 1. Core domain (`app/Payment/`)

- [x] 1.1 `git mv app/Payment/PaymentGateway.php app/Payment/PaymentProvider.php` and rename the class. Inside it: property `$is_manual` → `$is_offline`, method `is_manual()` → `is_offline()`, static `from_manual()` → `from_offline()`, local `$payment_gateway` → `$provider`. Leave `icon_url()`'s `/payments/kirki-%s/assets/logo.svg` path alone — the root `payments/` folder is not moving.
- [x] 1.2 `git mv app/Payment/Gateways app/Payment/Providers`, update the namespace in `PayPal.php` to `Kirki\Ecommerce\App\Payment\Providers`, change `extends PaymentGateway` → `extends PaymentProvider`, and update the hardcoded `$this->icon = app()->base_url('/app/Payment/Gateways/logo.svg')` to `/app/Payment/Providers/logo.svg`. Set `$this->is_offline = false` in place of `$this->is_manual = false`.
- [x] 1.3 `git mv app/Payment/ManualPaymentFactory.php app/Payment/OfflinePaymentFactory.php`, rename the class, and change its settings read from `$settings->get('payment_gateways')` to `$settings->get('offline_payments')`. Rename locals (`$manual_methods` → `$offline_providers`, `$payment_gateways` → `$providers`) and call `PaymentProvider::from_offline()`.
- [x] 1.4 In `app/Payment/PaymentManager.php`: `$gateways_registry` → `$providers_registry`; rename methods to `get_all_providers()`, `get_online_providers()`, `get_offline_providers()`, `get_available_providers()`, `get_available_online_providers()`, `get_available_offline_providers()`, `get_provider($id)`; swap `!$gateway->is_manual()` / `$gateway->is_manual()` for `is_offline()`; use `OfflinePaymentFactory::make()` and the renamed hook constant. Note `get_available_manual_gateways()` currently ends in `->to_array()` while its siblings use `->all()` — keep that as-is, it is out of scope.
- [x] 1.5 In `app/Constants/HookNames.php`, replace `ECOMMERCE_ALL_PAYMENT_GATEWAYS = 'kirki_ecommerce_all_payment_gateways'` with `ECOMMERCE_PAYMENT_PROVIDERS = 'kirki_ecommerce_payment_providers'`.
- [x] 1.6 Update the `@method` docblock in `app/Payment/Facades/Payment.php` to the seven renamed methods and the `PaymentProvider` return types.
- [x] 1.7 Update `app/Payment/WebhookController.php` for the renamed facade methods and the `{provider_id}` route parameter.
- [x] Verify: `php -l` passes on every file in `app/Payment/`; `grep -rn "PaymentGateway\|is_manual\|ManualPaymentFactory" app/Payment/` returns nothing; `cd resources/app && npm run typecheck && npm test`.

## 2. Bundled addon (`payments/kirki-stripe/`)

- [x] 2.1 In `payments/kirki-stripe/src/Stripe.php`: `use Kirki\Ecommerce\App\Payment\PaymentProvider`, `class Stripe extends PaymentProvider`, and `$this->is_offline = false` in place of `$this->is_manual = false`.
- [x] 2.2 In `payments/kirki-stripe/kirki-stripe.php`, change the filter to `add_filter('kirki_ecommerce_payment_providers', …)`.
- [x] 2.3 Confirm `payments/payments.json` needs no change (ids and `is_available` values are unchanged by this rename).
- [x] Verify: `php -l payments/kirki-stripe/src/Stripe.php payments/kirki-stripe/kirki-stripe.php`; `grep -rn "PaymentGateway\|all_payment_gateways\|is_manual" payments/ --exclude-dir=vendor` returns nothing; `cd resources/app && npm run typecheck && npm test`.

## 3. Services, controllers, requests, DTOs, resources

- [x] 3.1 `git mv app/Services/PaymentGatewayService.php app/Services/OnlinePaymentService.php`, rename the class, and rename its methods: `all_installable_gateways()` → `all_installable_providers()`, `__discover_installable_gateways()` → `__discover_installable_providers()`, `mock_download_gateway_zip()` → `mock_download_provider_zip()`. Repoint `find()` at `is_offline()`, use `Payment::get_provider()` / `Payment::get_online_providers()`, and update the `/app/Payment/Gateways/logo.svg` icon override for the `paypal` special case to `/app/Payment/Providers/logo.svg`. Leave the `__()` message strings untouched.
- [x] 3.2 `git mv app/Services/ManualPaymentMethodService.php app/Services/OfflinePaymentService.php`, rename the class, read/write the `offline_payments` settings key, and switch to `PaymentProvider::from_offline()` and the renamed DTOs. Note `get()` has a dead `$methods` local assigned then ignored — remove it, it becomes an orphan of this rename.
- [x] 3.3 `git mv` the two controllers to `app/Http/Controllers/Api/OnlinePaymentController.php` and `OfflinePaymentController.php`, rename the classes, and repoint their service, request, DTO, and resource imports. Leave every `__()` response message string exactly as it is today.
- [x] 3.4 `git mv app/Http/Requests/PaymentMethod app/Http/Requests/OfflinePayment`, rename both classes to `OfflinePaymentCreateRequest` / `OfflinePaymentUpdateRequest`, update the namespace, and change the `is_manual` rule and filter keys to `is_offline`.
- [x] 3.5 `git mv app/DTO/PaymentMethod app/DTO/OfflinePayment`, rename to `CreateOfflinePaymentDTO` / `UpdateOfflinePaymentDTO`, update the namespace, and rename the `$is_manual` property to `$is_offline`.
- [x] 3.6 `git mv app/Resources/PaymentGateway app/Resources/OnlinePayment` and rename the three classes to `InstallableOnlinePaymentListResource`, `OnlinePaymentListResource`, `OnlinePaymentResource`; `git mv app/Resources/PaymentMethod app/Resources/OfflinePayment` and rename to `OfflinePaymentListResource`, `OfflinePaymentResource`. In every one, emit `'is_offline' => $this->is_offline()` in place of `'is_manual'`. Keep the three byte-identical bodies duplicated — consolidation is explicitly out of scope.
- [x] 3.7 In `app/Http/Requests/Settings/SettingsUpdateRequest.php`, rename the `data.payment_gateways*` rules and filters to `data.offline_payments*` and the `.*.is_manual` entries to `.*.is_offline`.
- [x] Verify: `php -l` on every touched file; `grep -rn "PaymentGatewayService\|ManualPaymentMethodService\|PaymentMethodResource\|PaymentGatewayResource\|payment_gateways" app/` returns nothing; `cd resources/app && npm run typecheck && npm test`.

## 4. Routes

- [x] 4.1 In `routes/api.php`, update the imports to the renamed controllers and move the routes: `/payment-gateways{,/installable,/install,/{id},/download/{id}}` → `/online-payments…`, `/payment-methods{,/{id}}` → `/offline-payments…`.
- [x] 4.2 Change the webhook route parameter from `{gateway_id}` to `{provider_id}` and update `WebhookController::handle()` to read the renamed parameter.
- [x] 4.3 In `OnlinePaymentService::install()`, update the download URL built from `Route::url('payment-gateways/download/' . $id)` to `online-payments/download/` so it matches the moved route.
- [x] Verify: `php -l routes/api.php`; `grep -rn "payment-gateways\|payment-methods\|gateway_id" routes/ app/` returns nothing; `cd resources/app && npm run typecheck && npm test`.

## 5. Orders: database, model, actions, API

- [x] 5.1 In `database/migrations/CreateOrdersTable.php`: rename `payment_method` → `payment_provider` (string, 100), **delete** the `payment_gateway` column, rename `invoiced_payment_gateway_fee` → `invoiced_payment_provider_fee`, and rename index `idx_payment_method_tracking` → `idx_payment_provider_tracking`. Do **not** touch the `is_manual` column — it means "admin-created order".
- [x] 5.2 Update `app/Models/Order.php` `$fillable` and `$casts`: drop `payment_gateway`, rename `payment_method` → `payment_provider` and `invoiced_payment_gateway_fee` → `invoiced_payment_provider_fee`. Leave `is_manual` alone.
- [x] 5.3 Rename `OrderManager::set_payment_gateway_fee()` → `set_payment_provider_fee()` (and the `invoiced_payment_gateway_fee` key it writes), and update the `@method` line in `app/Facades/Order.php`.
- [x] 5.4 In `app/Actions/Order/CreateOrderAction.php`, replace the two lines assigning `payment_method` and `payment_gateway` with a single `$order_dto->payment_provider = $dto->payment_provider;`. Update `UpdateOrderAction.php` to `payment_provider`.
- [x] 5.5 In `app/Actions/Order/CreateRefundAction.php`, change `Payment::get_gateway($order->payment_gateway)` to `Payment::get_provider($order->payment_provider)` and rename the local `$payment_gateway` → `$provider`. In `UpdateRefundAction.php`, rename the `invoiced_payment_gateway_fee` read.
- [x] 5.6 Rename `payment_method` → `payment_provider` in the order DTOs (`CreateOrderDTO` — which also drops its `$payment_gateway` property — `CreateOrderPayloadDTO`, `UpdateOrderDTO`, `UpdateOrderPayloadDTO`), the order requests (`OrderCreateRequest`, `OrderUpdateRequest`, `OrderCalculationeRequest` — rules and filters), the order resources (`OrderResource`, `OrderListResource`), and the `sort_by` whitelist in `OrderController::index`.
- [x] 5.7 Update `database/seeders/SettingsSeeder.php::create_payment_settings()` to key the list `offline_payments` with `is_offline` on each entry, and `database/seeders/OrderSeeder.php` to `payment_provider`.
- [x] 5.8 Update the stale `Payment::get_gateway` references in the commented-out scratch lines of `app/Http/Controllers/Api/TestController.php` so they stay accurate.
- [x] Verify: `php -l` on every touched file; `grep -rn "payment_method\|payment_gateway" app/ database/ routes/` returns nothing; `cd resources/app && npm run typecheck && npm test`.

## 6. Frontend (`resources/app/`)

- [x] 6.1 In `schemas/catalog/payment.ts`, rename `PaymentGatewayFieldSchema` → `OnlinePaymentFieldSchema`, `PaymentGatewaySchema` → `OnlinePaymentSchema`, `PaymentGatewayListSchema` → `OnlinePaymentListSchema`, `PaymentMethodSchema` → `OfflinePaymentSchema`, the exported types to match, and the `is_manual` field to `is_offline`. Update `normalizeGatewayCollection` → `normalizeProviderCollection` and the doc comments' endpoint paths. Keep every `.nullish()`/`.passthrough()` exactly as-is — do not tighten while renaming. Update `schemas/catalog/payment.test.ts` in the same task.
- [x] 6.2 In `schemas/catalog/settings.ts`, rename `PaymentSettingsSchema`'s `payment_gateways` field to `offline_payments` and repoint it from `PaymentGatewaySchema` to `OfflinePaymentSchema` — it holds offline methods, so the current typing is wrong. Update `schemas/catalog/settings.test.ts` fixtures (`is_manual` → `is_offline`, `payment_gateways` → `offline_payments`).
- [x] 6.3 `git mv schemas/forms/payment-gateway-form.ts online-payment-form.ts` (and its `.test.ts`), renaming `PaymentGatewayEditFormSchema`/`Input`/`Payload` and `paymentGatewayEditDefaultValues` to their `OnlinePayment…` equivalents. Update the payload test alongside.
- [x] 6.4 `git mv schemas/forms/manual-payment-form.ts offline-payment-form.ts` (and its `.test.ts`), renaming the schema/type/exports to `OfflinePaymentForm…`, and changing both the shape field and the transform output from `is_manual` to `is_offline`. Per the repo's zod v3 constraints, keep `.default(true)` exactly where it already was — do not add or move any `.default()`. Update the payload test in the same task.
- [x] 6.5 In `libs/endpoints.ts` rename the six payment constants and their paths to `/online-payments` and `/offline-payments`; in `libs/query-keys.ts` rename `PaymentGateways`/`InstallablePaymentGateways`/`PaymentGateway`/`PaymentMethods` to their `OnlinePayment…`/`OfflinePayment…` equivalents, including the literal key strings.
- [x] 6.6 In `services/payment.ts`, rename every fetcher, mutation, and hook (`getPaymentGateways` → `getOnlinePayments`, `usePaymentMethodsQuery` → `useOfflinePaymentsQuery`, etc.), the schema imports, and the hardcoded `invalidateQueries` key literals — which must match the renamed `queryKeys` values or cache invalidation silently stops working. Leave every `__()` toast string exactly as it is.
- [x] 6.7 Update the `types/index.ts` re-export line to the renamed `OnlinePayment` / `OfflinePayment` types.
- [x] 6.8 `git mv` the five page components: `payment-gateway.tsx` → `online-payment.tsx`, `payment-gateway-dialog.tsx` → `online-payment-dialog.tsx`, `payment-gateway-edit-dialog.tsx` → `online-payment-edit-dialog.tsx`, `manual-payment.tsx` → `offline-payment.tsx`, `manual-payment-dialog.tsx` → `offline-payment-dialog.tsx`. Rename each component and its `displayName`, props type, and local variables, and update the imports in `payment-settings.tsx`. Keep every `__()` string as-is, including "Payment gateways", "Manual payment methods", and "Available Payment Gateways".
- [x] 6.9 In `pages/settings/payment-settings/utils.tsx`, rename `DynamicGatewayFields` → `DynamicOnlinePaymentFields` and the `GatewayField`/`DynamicGatewayFieldsProps` types, updating the import in the edit dialog.
- [x] 6.10 In `online-payment-dialog.tsx`, wrap the bare `Coming Soon` literal in `__('Coming Soon', 'kirki-ecommerce')` — every other label in the file already is. No wording change.
- [x] 6.11 Leave `preview-pages/table-preview.tsx` alone: its `paymentMethod` field is local mock data, not an API contract.
- [x] Verify: `grep -rn "PaymentGateway\|PaymentMethod\|payment_gateways\|is_manual\|payment-gateways\|payment-methods" resources/app --exclude-dir=node_modules` returns nothing outside `__()` display strings; `cd resources/app && npm run typecheck && npm test`.

## 7. API docs and PHP tests

- [x] 7.1 `git mv docs/ecommerce/payment-gateways docs/ecommerce/online-payments` and update all 7 files: `folder.yml` display name to "Online Payments", every `{{baseUrl}}/payment-gateways…` URL to `/online-payments…`, and every example body's `is_manual` to `is_offline`.
- [x] 7.2 `git mv docs/ecommerce/payment-methods docs/ecommerce/offline-payments` and update all 6 files the same way, to "Offline Payments" and `/offline-payments…`.
- [x] 7.3 In `docs/ecommerce/settings/payment.yml`, rename the example body's `payment_gateways` key to `offline_payments` and each entry's `is_manual` to `is_offline`.
- [x] 7.4 Grep `docs/ecommerce/orders/` for `payment_method` in example request and response bodies and rename to `payment_provider`.
- [x] 7.5 Update `tests/Integration/OrderApiTest.php` — `'payment_method' => 'stripe'` becomes `'payment_provider' => 'stripe'`, plus any assertion reading the old key.
- [x] Verify: `grep -rn "payment-gateways\|payment-methods\|payment_gateways\|payment_method\|is_manual" docs/ tests/` returns nothing; `cd resources/app && npm run typecheck && npm test`.

## 8. Full verification gate

- [x] 8.1 `cd resources/app && npm run typecheck && npm test` — clean.
- [x] 8.2 `php -l` across every changed PHP file (`git diff --name-only main -- '*.php' | xargs -n1 php -l`).
- [x] 8.3 Repo-wide grep gate — zero hits outside `libraries/framework/`, `vendor/`, `node_modules/`, and `__()` display strings, for: `payment_gateways`, `PaymentGateway`, `ManualPayment`, `PaymentMethod`, `payment-gateways`, `payment-methods`, `get_gateway`, `all_payment_gateways`, and `is_manual` (excluding the `orders.is_manual` column and its model/DTO/request/resource plumbing).
- [x] 8.4 Verified via the **integration test database** instead: it is built from scratch each run, and `SHOW COLUMNS` confirms `payment_provider`, `invoiced_payment_provider_fee`, no `payment_gateway`, `is_manual` intact, and index `idx_payment_provider_tracking`. Deliberately did NOT run `SettingsSeeder` against the dev DB — it overwrites all seven settings sections, not just payment, which is more destructive than this change warrants. Dev DB still needs a manual orders re-migrate.
- [x] 8.5 Ran the full Integration suite in Docker before and after the change. Both runs: 167 tests / 1748 assertions / 34 failures, and the sorted list of failing test names is **byte-identical**. The 34 failures are pre-existing and unrelated (e.g. customer create returns 422); `OrderApiTest` was already failing at HEAD, so it could not be used as a green gate.
- [x] 8.6 Verified by enumerating the live WP REST route table instead of curl (endpoints are auth-gated): all eight payment routes register under `/online-payments*` and `/offline-payments*`, the webhook takes `{provider_id}`, and no legacy path remains.
- [x] 8.7 `kirki-stripe` is not installed in this dev WP (only `kirki-ecommerce` is active), so Stripe could not be resolved live. Proved the equivalent wiring with bundled PayPal, which registers through the same renamed hook: `Payment::get_provider('paypal')` returns `Kirki\Ecommerce\App\Payment\Providers\PayPal` with `is_offline=false`, `HookNames::ECOMMERCE_PAYMENT_PROVIDERS` reads `kirki_ecommerce_payment_providers`, `PaymentProvider` exists and the old `PaymentGateway` class does not.
