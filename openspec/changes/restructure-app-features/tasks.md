All commands run from `resources/app/`. Every file move uses `git mv` so blame survives. No task in this change may alter a statement inside a moved file — only its import block.

## 1. Preparation: codemod and boundary rules

- [x] 1.1 Write `scripts/migrate-feature.mjs`: given a feature name and a move manifest (old path → new path), it rewrites `@/`-alias imports across the whole app, updates relative imports inside moved files, and reports any path it could not resolve rather than guessing. Model it on the existing `scripts/migrate-pages.mjs` / `fix-page-paths.mjs` prior art.
- [x] 1.2 Add the boundary rules to `eslint.config.js` at severity `warn`: (a) no import of `@/features/<x>/**` from outside `features/<x>/`, the barrel `@/features/<x>` being the sole permitted entry; (b) no import of `@/features/*` from any of `components/`, `hooks/`, `utils/`, `libs/`, `theme/`, `types/`, `schemas/`, `services/`, `contexts/`, `config/`. Generated per-feature from a `FEATURES` array rather than hand-written 12 times.
- [x] 1.3 Add `eslint-plugin-import` (+ `eslint-import-resolver-typescript`, needed for the resolver to understand the `@/` alias) and enable `import/no-cycle` at `error` (not gated by the warn/error migration toggle — a cycle is always a real defect). Baseline violation count: 0.
- [x] 1.4 Create `features/` with the 12 empty feature directories, each with a placeholder `index.ts`, so later commits only add to an existing skeleton.
- [x] 1.5 Verify: `npm run typecheck && npm test` — both clean (378/378 tests, 59 files). `npm run lint` baseline: 1 pre-existing error unrelated to this change (`object-shorthand` in `pages/products/product-form/sections/right-panel/categories/categories.tsx`, present on `dev` before this branch — confirmed via `git log`), 0 warnings from the new boundary/cycle rules (expected, since nothing imports `@/features/*` yet).

## 2. Shared-layer splits

Nothing may move into a feature before this group lands — it is the dependency root for every later commit.

- [x] 2.1 Delete `types/index.ts`. Rewrote all importing files (179, matching the proposal's count exactly) via a purpose-built codemod (`scripts/inline-types-barrel.mjs`) that maps every barrel-exported symbol to its real defining module and splits mixed-source import statements accordingly, preserving `type`/value distinctions and `as` aliases. `npm run typecheck` is 0 errors.
- [x] 2.2 Delete `types/entities/` (18 files). `UnitPriceValue`/`UpdateVariantsPayload` (the two real declarations inside `types/entities/product.ts`, not re-exports) and `ToastVariant` (`types/entities/toast.ts`) had no schema to import from — see correction note below.
- [x] 2.3 Move `types/filters/{product,order,coupon}.ts` to `features/{products,orders,coupons}/types.ts` (content unchanged, only relocated + import paths fixed). For `products`, merged into the `types.ts` already created in 2.2 for `UnitPriceValue`/`UpdateVariantsPayload`.
- [x] 2.4 `types/components/{common,icon}.ts`, `types/list-state.ts`, `types/api/{response,result}.ts`, `types/pages/*`, `types/common-actions.ts` stay at root as deep-import targets (no barrel file re-exports them — a barrel would just recreate the god-barrel at smaller scope, so importers use the deep path directly, e.g. `@/types/list-state`).
- [x] 2.5 Split `libs/query-keys.ts` into `features/<f>/services/query-keys.ts` for every feature except the 3 that stay root-owned (`Countries`/`Settings`/`DefaultSettings`, kept in a trimmed root `libs/query-keys.ts`). Every group has an `all` root; `lists()`/`details()` sub-roots nest under it so invalidation can target exactly the old scope (list-only vs detail-only) while still being a strict prefix of the params/id-bearing leaf functions. This is a deliberate key-shape change from the original flat strings (`'Products'` vs `'Product'` were different top-level literals; cache keys are pure-internal so this is invisible behaviorally). Settings' 5 sub-domain groups (`shippingKeys`, `taxKeys`, `currencyKeys`, `paymentKeys`, `schemaProfileKeys`) live in one `features/settings/services/query-keys.ts` for now — pushed down into sub-feature dirs in group 7 when those services move. `services/page.ts` (dead code, zero importers, slated for deletion in 8.3) got its own tiny local `pageKeys` rather than a feature — it doesn't belong to any of the 12 features.
- [x] 2.6 Replaced all raw literal invalidations found by an app-wide sweep for `queryKey: ['` — 69 in `services/*.ts` (more than the ~61 estimate; the difference is `services/coupon.ts`'s two ad-hoc read keys `CouponValidate`/`CouponNewCode`, never in the original factory, now nested under `couponKeys`) plus the 2 in `pages/settings/shipping-settings/shipping-profile/shipping-profile.tsx` and `pages/settings/tax-settings/tax-profile/tax-profile.tsx` (optimistic-update call sites using `useQueryClient()`/a module-level `queryClient` directly, outside any service). Verified via `grep -rn "queryKey: \['"` returning zero matches. Every prefix relationship was checked by hand against each mutation's original invalidation scope (list-only vs detail-only vs both) rather than collapsed to a single broad `.all` call, to guarantee identical invalidation behavior — see design.md's flagged highest-risk-edit warning.
- [x] 2.7 Wired: `product.ts` → `inventoryKeys.all` (via `@/features/inventory` barrel); `bulk-edit.ts` → `inventoryKeys.all` + `productKeys.lists()` (via `@/features/inventory` and `@/features/products` barrels) — `lists()` not `all`, since the original `bulk-edit.ts` invalidation only ever touched the Products list cache, never the detail cache, and `productKeys.all` would have been strictly broader than that.
- [x] 2.8 Moved both contexts; rewrote their 6 importers (bulk-edit: 3 files, inventory: 3 files) to import the provider/hook via the feature barrel rather than the old `@/contexts/*` path.
- [x] 2.9 Confirmed: `services/helpers.ts`, `services/country.ts`, `services/settings.ts` are the only ones with no feature-move task in groups 3–8.
- [x] 2.10 `npm run typecheck && npm test` — 0 errors, 378/378 tests, 59 files, content unchanged. `npm run lint`: 1 pre-existing error (same baseline as 1.5, unrelated to this change), 22 warnings — all expected transient `no-restricted-imports` warnings from the 16 not-yet-moved root services reaching into `@/features/*`; each disappears when that service moves in groups 3–8. `npm run build` also verified clean as extra insurance on this highest-risk stage.

**Correction during implementation** (recorded per `openspec/config.yaml` rule): `UnitPriceValue`, `UpdateVariantsPayload` (product-specific, also used by `bulk-edit` — folded into `features/products/types.ts`, created ahead of the rest of the `products` move since `bulk-edit`'s context needs it now) and `ToastVariant` (genuinely cross-feature — folded into the surviving `types/pages/common.ts` next to the related `ToastMessageConfig`) are not mentioned in design.md's `types/` disposition table because that table only accounted for entity files that were pure re-exports. These three are the exceptions.

## 3. Clone features — brands, tags, categories, collections

Four structurally identical features (~24 files total). They exercise the codemod on the smallest blast radius and cover the same code paths four times. One commit each.

- [x] 3.1 `brands`: moved (`brands.tsx`→`pages/`, `new-brand.tsx`+`brand-add-edit-dialog.tsx`+`brand-table/`→`components/`, `services/brand.ts`, `schemas/catalog/brand.ts`, `schemas/forms/brand-form.ts`+test to `tests/schemas/forms/` per the tests-mirror-source requirement). Barrel exports `BrandAddEditPopover`, `useBrandsQuery`, `brandKeys`, `Brand` type; the 3 `products` call sites that used deep paths now import from `@/features/brands`.
      **Codemod bug found and fixed**: `migrate-feature.mjs`'s `resolveSpecifier` checked the filesystem *after* all `git mv`s in the batch had already run, so it could never resolve a specifier pointing at another file moved in the same batch (e.g. `brand-add-edit-dialog.tsx` importing `schemas/catalog/brand`, which moved in the same manifest) — 24 unresolved specifiers on the first run. Fixed by consulting the move manifest first (exact old-path match) before falling back to `fs.existsSync`. Re-ran clean: 0 unresolved.
      **Design gap found and resolved**: `routes.tsx`'s `lazy(() => import('@/features/brands/pages/brands'))` is a deep import into the feature from outside it — a real boundary-rule hit that no task anticipated, since brands (and the other three clone features) get no `routes.tsx` of their own. Route composition needs a concrete file target for its own code-split chunk, which is a structurally different dependency than internal reuse, so added a `kirki/route-manifest-lazy-imports` eslint block turning `no-restricted-imports` back off for `**/routes.tsx` files (ordered after the boundary configs, since flat config resolves per-rule by last match). Broadened the existing `kirki/route-manifest` block's `files` from `'routes.tsx'` to `'**/routes.tsx'` at the same time so it also covers the per-feature route files added in later groups.
      Also added directory-pruning to `migrate-feature.mjs` after the move loop: git doesn't track empty directories, so a fully-emptied source directory (e.g. `pages/brands/brand-table/` once all 3 of its files moved out) was left behind on disk; now walked and removed up to `APP_ROOT`.
- [x] 3.2 `tags`: moved, plus `components/form/tags-field.tsx` → `features/tags/components/fields/`. Barrel exports `TagsField`, `tagKeys`. 1 cross-feature call site fixed (`pages/products/product-form/sections/right-panel/tags.tsx`).
- [x] 3.3 `categories`: moved. Barrel exports `useCategoriesQuery`, `useCreateCategoryMutation`, `categoryKeys`, `Category` type. 7 cross-feature call sites fixed (6 in products, 1 in settings/shipping-rule-form-card.tsx — categories turned out to be consumed outside products too).
- [x] 3.4 `collections`: moved, plus `components/form/collections-field.tsx` → `features/collections/components/fields/`. Barrel exports `CollectionsField`, `useCollectionsQuery`, `collectionKeys`. `collection-details.tsx` flattened out of its single-file `collection-details/` subfolder into `pages/` directly, matching the anatomy's `pages/` being route components, not a folder-per-route wrapper. 3 cross-feature call sites fixed in `products`.
- [x] 3.5 Codemod bug found on the very first run (brands) and fixed before proceeding — see 3.1 note. No further unresolved paths on brands; tags/categories/collections not yet attempted.
- [x] 3.6 `npm run typecheck && npm test` after each of the four moves — all clean, 378/378 tests throughout.

## 4. Small features — customers, coupons, inventory, bulk-edit

- [x] 4.1 `customers`: moved `pages/customers/**`, `services/customer.ts`, catalog + form schemas and their tests into `features/customers/`. Barrel exports `BillingAddress`, `ShippingAddress`, `CustomerOverview` (the three components `orders`' `add-customer-dialog.tsx` embeds), plus `useCreateCustomerMutation`/`useCustomerQuery`/`useCustomersQuery`, `customerKeys`, `CustomerFormSchema`, `CustomerAddressSchema`, and the `Customer`/`CustomerAddress`/`CustomerFormInput`/`CustomerFormPayload` types — `schemas/catalog/order.ts` (root, pre-group-5) needs `CustomerAddressSchema` for its `shipping_address`/`billing_address` fields.
      **Found and fixed a pre-existing test-infra gap**: `vitest.setup.ts`'s comment already promised to stub `window.kirki_ecommerce` (so import-time reads don't throw outside a browser) but the code only stubbed `window` itself. Latent until now because nothing had previously imported a feature barrel from a plain schema module — `schemas/catalog/order.ts` importing `CustomerAddressSchema` via `@/features/customers` transitively evaluates the barrel's re-exported service hooks, which reach `libs/api.ts` → `conf.ts`'s `window.kirki_ecommerce.rest_url_base` at module-eval time. Completed the stub rather than routing around it with a deep import (which group 9 would later flag as a permanent boundary violation).
- [x] 4.2 `coupons`: moved `pages/coupons/**`, `services/coupon.ts`, `schemas/catalog/coupon.ts`, `schemas/forms/coupon-form.ts` + test into `features/coupons/`. `edit-coupon/config/{coupon-badge,coupon-datetime}.ts` → `features/coupons/lib/`. The pre-existing `coupon-form.ts` → `coupon-datetime` import is now intra-feature. Barrel exports `couponKeys` and `CouponSchema` (for `schemas/catalog/order.ts`'s `discount_details` field — same transient root→feature pattern as 4.1's `CustomerAddressSchema`, resolves once `order.ts` moves in group 5).
- [x] 4.3 `inventory`: moved `pages/inventory/**`, `services/inventory.ts` into `features/inventory/` (the skeleton, context, and `inventoryKeys` barrel export already existed from the group 2 context move). `pages/inventory/utils.tsx` (JSX-bearing table-header config) → `lib/utils.tsx`, relocated as-is.
- [x] 4.4 `bulk-edit`: moved `pages/bulk-edit/**`, `services/bulk-edit.ts` into `features/bulk-edit/`; `hooks/useBulkEditList.ts` → `features/bulk-edit/hooks/use-bulk-edit-list.ts` (its only consumer, `single-row.tsx`, moved with it — removed from the shared `hooks/index.ts` barrel since it's feature-owned now, not shared). Barrel exports `bulkEditKeys` and `useUpdateBulkVariantsMutation` (consumed by `inventory`'s page).
      `hooks/useMarkList.ts` deliberately left alone — genuinely shared generic list-selection state consumed by `brands`, `categories`, `collections`, `customers`, `inventory`, `tags`, a settings page, and `components/data-table/data-table-selection-context.tsx`, not bulk-edit domain logic. Moving it would have made every one of those a bulk-edit consumer through the boundary barrel, which is backwards.
      **Codemod bug found and fixed (second one, distinct from 3.1's)**: `migrate-feature.mjs`'s `toAliasSpecifier`/`toRelativeSpecifier` reconstructed a specifier from the resolved file's path verbatim, so any target resolved via directory-index lookup (e.g. `@/theme` → `theme/index.ts`) got rewritten with an explicit `/index` suffix (`@/theme/index`) — functionally identical (`tsc`/vitest never flagged it) but inconsistent with every hand-written import in the codebase. Present in every commit since group 3 (46 occurrences across brands/tags/categories/collections/customers/coupons/inventory, all in already-committed files). Fixed the two specifier-builder functions to strip a trailing `/index` segment, then swept the 46 existing occurrences with the same rule in this commit so the fix is retroactive rather than leaving old and new style mixed.
- [x] 4.5 Verified after each of the four commits: `npm run typecheck` (0 errors throughout), `npm test` (378/378, 59 files, unchanged), `npm run lint` (0 errors; warnings shrank from 18 → 13 as each service moved out from under the root `no-restricted-imports` rule).

## 5. Orders

- [x] 5.1 Move `pages/orders/**` → `features/orders/{pages,components}/`, `services/order.ts` → `features/orders/services/`, `schemas/catalog/order.ts` and the order form schemas + tests → `features/orders/schemas/`.
- [x] 5.2 Move `order-details/config/{order-actions,order-address,order-badge}.ts` and `order-create/config/customer-address.ts` → `features/orders/lib/`; `order-create/types.ts` folds into `features/orders/types.ts`. Kept the `OrderItem` name as-is (no actual collision with the schema's `OrderItem` — different files, and neither is re-exported through the same barrel entry).
- [x] 5.3 Confirmed the pre-existing `services/order.ts` → `order-actions` import is now intra-feature.
- [x] 5.4 Already correct from group 4 — `order-create/components/customer/add-customer-dialog.tsx` already imported customers via `@/features/customers` when the customers feature moved.
- [x] 5.5 Added `features/orders/routes.tsx` (`/orders`, `/orders/create`, `/orders/:id`) and wired it into root `routes.tsx` via `...ordersRoutes` — not deferred to group 8, since an unused per-feature `routes.tsx` would be dead code for three more stages.
- [x] 5.6 Verify: `npm run typecheck && npm test && npm run lint && npm run build` — all clean. lint: 1 pre-existing unrelated error, 9 warnings (down from 13; `schemas/catalog/order.ts`'s coupons/customers imports are no longer flagged at all now that the file itself lives inside `features/orders/`).

## 6. Products

Largest single feature (51 files) and the hub for most cross-feature edges.

- [x] 6.1 Move `pages/products/**` → `features/products/{pages,components}/`, `services/{product,attribute}.ts` → `features/products/services/`, and the product/variant/attribute catalog schemas plus the product form schemas and their tests.
      **Correction**: the "6 product form schemas" estimate undercounted — the actual set consumed exclusively (or, for `add-variation-form`/`variation-value-form`, primarily) by products is 10: `product-form`, `product-basics-form`, `product-additional-info-form`, `product-seo-form`, `product-attribute-form`, `add-variation-form`, `variation-value-form`, `base-unit-form`, `product-add-category-form`, `product-variation-popover-form` (`products-settings-form` is a different, settings-owned file despite the similar name — stays put for group 7). `add-variation-form`/`variation-value-form` turned out to also be consumed by `pages/settings/essential-settings/variation-library/**` (9 files) — same "consumed outside its home page too" pattern as `categories` in group 3 — so those 9 files now reach the moved schemas, `useAttributeQuery`-family hooks, `Attribute`/`AttributeValue` types, and `lib/seo-settings/utils`'s `groupDetails`/`optionsList`/`requiredFields` (via `add-schema-dialog.tsx`) through the `@/features/products` barrel.
      **Design gap found and resolved**: `schemas/catalog/app-config.ts` (permanently root-level, feeds the 3 root-kept services) imported `ProductCurrencySchema` from `@/schemas/catalog/product` — invisible before this commit since that was a schemas/-to-schemas/ import, but a real `shared code depends on a feature` violation the instant `product.ts` moved. Neither "move app-config into products" (it's genuinely app-wide) nor "route through the products barrel" (shared root code may never import `@/features/*`, no exception) fit. Resolved by giving `app-config.ts` its own local `AppConfigCurrencySchema` duplicating the same 4-field shape — a few lines of duplication, zero behavioral change (identical validated shape), full severance of the wrongful edge.
- [x] 6.2 Move `products/utils.ts`, `products/variant-matrix.ts`, `product-form/sections/price/utils.ts`, `product-form/sections/seo-settings/utils.ts` → `features/products/lib/`; their tests (`variant-matrix.test.ts`, `price/utils.test.ts`) → `features/products/tests/lib/`. The two section-nested `utils` files kept one level of nesting (`lib/price/utils.ts`, `lib/seo-settings/utils.ts`) rather than flattening — `products/utils.ts` already claims the bare `lib/utils.ts` name, and three distinct files can't share one flat filename.
- [x] 6.3 Move `components/form/attribute-values-field.tsx` (+ its sibling `attribute-value-types.tsx`) and `components/shared/select-products-dialog.tsx` + `select-products-dialog/` → `features/products/components/`, resolving their imports of `variation-dialog` and the three product-table filters into intra-feature imports. `select-products-dialog` turned out to also be consumed by `orders` (`order-create.tsx`, `types.ts`) — barrel-routed.
- [x] 6.4 Relocate `pages/utils.ts` (`calculateProfit`, `normalizeErrors`, plus `dateFormatter`/`makeSuggestionList`/`dispatchToastMessage` — all 5 exports, not just the two named): consumed across products, bulk-edit, inventory and settings, confirming no single feature owns it. Moved as one file (no internal split — that would be a content change beyond relocation) to `utils/common.ts`, matching `types/pages/common.ts`'s precedent for "genuinely cross-feature, no better subject name" groupings. Deleted `pages/utils.ts`.
- [x] 6.5 Point `products` → `brands` imports (brand dialog) at the feature barrel — already done in group 3 when `brands` moved. `products` → `settings` (tax-profile dialog, shipping-profile dialog) imports still go through `@/pages/settings/...` deep paths, which is not a boundary violation yet since `settings` isn't a feature until group 7 — nothing to fix here now.
- [x] 6.6 Added `features/products/routes.tsx` (`/products`, `/products/create`, `/products/:id`) and wired it into root `routes.tsx` via `...productsRoutes`, same as group 5's `ordersRoutes` — not deferred to group 8.
- [x] 6.7 Verify: `npm run typecheck && npm test && npm run lint && npm run build` — all clean. lint: 1 pre-existing unrelated error, 5 warnings (down from 9; only the 5 services still waiting on the settings move remain).

## 7. Settings

98 files, 11 sub-features, nested one level deep. Each sub-feature takes the same anatomy.

- [x] 7.1 Created `features/settings/` shell: `pages/settings-layout.tsx` (+ `settings-sidebar.tsx`, `settings-nav-item.tsx`, `settings-page-header.tsx`), `hooks/use-settings-page-actions.ts`, `lib/utils.tsx` (from `pages/settings/utils.tsx`, incl. `setUnsavedDataStatus`).
- [x] 7.2 Moved `shipping/` (42 files, not 23 — the estimate undercounted the same way products' "6 form schemas" did): pages, components, `services/shipping.ts`, shipping catalog + form schemas and tests. `shipping-settings/utils.tsx` (262L, mixes types + logic) split into `features/settings/shipping/lib/utils.tsx` and `types.ts`, with the 13 consumer files' mixed type+value import statements split accordingly; `shipping-profile/utils.ts` + its test → `lib/shipping-profile/`; `shipping-method/shipping-rules/helper.ts` → `lib/shipping-rules/` (nested one level, not flattened — `products/utils.ts` already claims the bare `lib/utils.ts` name and multiple `helper.ts`/`utils.ts` files across sub-directories can't share one flat filename, same resolution as products' price/seo-settings utils in group 6).
- [x] 7.3 Moved `components/form/shipping-box-field.tsx` and `components/form/regions-field.tsx` (+ their supporting `components/shipping-box-preview/`, not called out in the task text but consumed only by these two) → `features/settings/shipping/components/`, resolving their dialog and utils imports intra-feature. Only `ShippingBoxField` has outside consumers (`bulk-edit`, `products`) — exported from `features/settings/index.ts`; `RegionsField` has none, so per the "barrel exports only what's actually consumed" principle it stays unexported.
- [x] 7.4 Moved `tax/` (37 files, not 22 — same undercount pattern): pages, components, `services/tax.ts`, schemas and tests; `tax-settings/{utils.ts,helper.ts}` → `lib/`, `tax-region/tax-rules/helper.ts` → `lib/tax-rules/` (nested, avoids colliding with the other `helper.ts`). Left the three direct `useQueryClient` uses in `tax-profile.tsx`, `general-edit-region.tsx`, `edit-region-eu.tsx` as-is: all three already invalidate through the correctly declared key (`taxKeys.all` / `settingsKeys.section('tax')`, from group 2's query-key work) inside a manual optimistic-update sequence (cancel/get/set query data before the request, matching group 2's `shipping-profile.tsx` pattern) that the generic `useUpdateSettingsMutation` hook doesn't support — replacing them would change behavior, so recorded for `extract-feature-logic-hooks` instead.
- [x] 7.5 Moved `essentials/` (12 files, incl. `schema-profile/` and `variation-library/`) with `services/schema.ts` and their schemas/tests. `add-variation-form`/`variation-value-form` (owned by `products`, per group 6) turned out to be consumed by 9 `variation-library/**` files here — barrel-routed through `@/features/products`, same as `add-schema-dialog.tsx`'s SEO-utils import.
- [x] 7.6 Moved `multi-currency/` (9 files + 2 more form schemas missed by the initial sweep — `exchange-rate-form.ts`, `add-currency-popup-form.ts`, named unlike the rest of the multi-currency set so a name-based file search skipped them) with `services/currency.ts`, and `payment/` (7 files, incl. `payment-settings/utils.tsx` which is actually a JSX component — `DynamicOnlinePaymentFields` — not pure logic, so it went to `components/` rather than `lib/`) with `services/payment.ts`, plus their schemas and tests.
- [x] 7.7 Moved the remaining sub-features: `general/` (7, incl. `utils.ts` → `lib/` — pure data, fits the anatomy same as email's), `email/` (5, incl. `utils.ts` → `lib/`), `products/` (3), `checkout/` (3), `advanced/` (1).
      **Design gaps found and resolved** (both discovered once every settings sub-feature had moved and `services/settings.ts` / `schemas/catalog/settings.ts` — both permanently root-kept — could no longer avoid touching `@/features/settings`): `services/settings.ts`'s `SettingsPayloadMap` and `schemas/catalog/settings.ts`'s `SettingsSchemaMap` each structurally name every settings section's real form/catalog schema by design (that's their whole job), so unlike `app-config.ts`'s small 4-field `ProductCurrencySchema` case in group 6, duplicating them isn't a reasonable fix — `ShippingZoneSchema` and `TaxRegionSchema` in particular are large and change with their owning sub-feature. Both are the same "genuine wart" design.md already records for `services/settings.ts` staying root; documented with an inline `eslint-disable-next-line no-restricted-imports` and an explanatory comment on each rather than distorting the schemas or leaving the violation unexplained.
- [x] 7.8 Added `features/settings/routes.tsx` with the nested `SettingsLayout` parent, the index redirect to `/settings/general`, and all 18 children, wired into root `routes.tsx` via `...settingsRoutes` (not deferred to group 8, matching groups 5/6). `RouteConfig.Settings.LicenseSettings` left unregistered as it was before — not fixed.
- [x] 7.9 Verify: `npm run typecheck && npm test && npm run lint && npm run build` — all clean. lint: 1 pre-existing unrelated error, **0 warnings** — every one of the 16 not-yet-moved root services from group 2's baseline has now moved; the only two `@/features/*` imports left in shared root code are the two documented exceptions above.

## 8. System feature and dead-code removal

- [x] 8.1 Moved `pages/not-found/**` and `pages/coming-soon/**` → `features/system/pages/`, added `features/system/routes.tsx` (`/analytics`, `/report`, `/tools`, and the `*` catch-all, keeping `NotFound` eagerly imported as it is today).
- [x] 8.2 `pages/` was already fully empty (every remaining sub-tree had moved in groups 3–7); confirmed no `@/pages/` import remains anywhere.
- [x] 8.3 Deleted `services/page.ts`, `schemas/catalog/page.ts` (zero importers, verified). `types/entities/page.ts` no longer applies — `types/entities/` as a whole was already deleted in group 2.
- [x] 8.4 Deleted `preview-pages/` (45 files), `tryouts.tsx`, and their `eslint.config.js` `kirki/preview-sandboxes` override block.
- [x] 8.5 In the archived `openspec/changes/archive/2026-08-12-stacked-items-primitive/tasks.md` (the change was archived, un-committed, before this session started — same relative task numbering), marked task 7.3 obsolete with the reason (preview harness removed by this change). Left the other pending QA tasks in `stacked-items-primitive`, `rule-items-primitive` and `variant-matrix-regeneration` open, each annotated with the specific new file paths to check against.
- [x] 8.6 Deleted the 15 spent one-off codemods in `scripts/`. Kept `scripts/babel-plugin-scoped-auto-label.js` (`vite.config.js` loads it). Also deleted this change's own now-spent one-off artifacts by the same logic — `scripts/inline-types-barrel.mjs` (hardcoded to the now-deleted `types/index.ts`) and `scripts/manifests/*.json` (one-off move manifests, superseded by git history) — while keeping `scripts/migrate-feature.mjs`, which is generic and reusable, not one-off.
- [x] 8.7 Rewrote root `routes.tsx` to compose all 12 features' route arrays inside the existing `UnsavedChangesController` layout route. Groups 3–4's eight features (brands, tags, categories, collections, customers, coupons, inventory, bulk-edit) never got their own `routes.tsx` in their own stage — added all eight here so every feature is composed the same way, rather than leaving some routes inlined in root indefinitely.
- [x] 8.8 Verify: `npm run typecheck && npm test && npm run lint && npm run build` — all clean. lint: 1 pre-existing unrelated error, 0 warnings.

## 9. Enforcement and final verification

- [ ] 9.1 Promote both boundary rules from `warn` to `error` in `eslint.config.js` and confirm `npm run lint` is clean at zero violations.
- [ ] 9.2 Confirm `import/no-cycle` reports zero — the same count recorded in 1.3. Any cycle here was introduced by a barrel and must be fixed by narrowing that barrel, not by suppressing the rule.
- [ ] 9.3 Audit every `features/*/index.ts`: each export must correspond to a real cross-feature import. Delete any speculative export — a barrel that exposes the whole feature passes lint while restoring the coupling this change removes.
- [ ] 9.4 Add `knip` with config, run it, and act on what it reports. Record anything intentional rather than silently ignoring it.
- [ ] 9.5 Run `npm run build` and diff the emitted chunk list under `../../assets` against a pre-change build — each feature's routes must still resolve to their own dynamic chunks.
- [ ] 9.6 Review the whole branch with `git diff -M dev...HEAD`: every moved file must show import-block changes only. A moved file with altered statements is a defect in this change.
- [ ] 9.7 Confirm all 59 original test files are present at their mirrored paths with unchanged content, and that `npm test` passes.
- [ ] 9.8 Final verification: `npm run typecheck && npm test`, plus `npm run lint && npm run build`.
